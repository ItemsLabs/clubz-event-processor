// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBanners(t *testing.T) {
	t.Parallel()

	query := Banners()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBannersDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBannersQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Banners().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBannersSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BannerSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBannersExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BannerExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Banner exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BannerExists to return true, but got false.")
	}
}

func testBannersFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	bannerFound, err := FindBanner(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if bannerFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBannersBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Banners().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testBannersOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Banners().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBannersAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	bannerOne := &Banner{}
	bannerTwo := &Banner{}
	if err = randomize.Struct(seed, bannerOne, bannerDBTypes, false, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}
	if err = randomize.Struct(seed, bannerTwo, bannerDBTypes, false, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = bannerOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = bannerTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Banners().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBannersCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	bannerOne := &Banner{}
	bannerTwo := &Banner{}
	if err = randomize.Struct(seed, bannerOne, bannerDBTypes, false, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}
	if err = randomize.Struct(seed, bannerTwo, bannerDBTypes, false, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = bannerOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = bannerTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testBannersInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBannersInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(bannerColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBannerToManyUserBanners(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Banner
	var b, c UserBanner

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, userBannerDBTypes, false, userBannerColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userBannerDBTypes, false, userBannerColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.BannerID = a.ID
	c.BannerID = a.ID

	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.UserBanners().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.BannerID == b.BannerID {
			bFound = true
		}
		if v.BannerID == c.BannerID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := BannerSlice{&a}
	if err = a.L.LoadUserBanners(tx, false, (*[]*Banner)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserBanners); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.UserBanners = nil
	if err = a.L.LoadUserBanners(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserBanners); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testBannerToManyAddOpUserBanners(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Banner
	var b, c, d, e UserBanner

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, bannerDBTypes, false, strmangle.SetComplement(bannerPrimaryKeyColumns, bannerColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*UserBanner{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, userBannerDBTypes, false, strmangle.SetComplement(userBannerPrimaryKeyColumns, userBannerColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*UserBanner{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddUserBanners(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.BannerID {
			t.Error("foreign key was wrong value", a.ID, first.BannerID)
		}
		if a.ID != second.BannerID {
			t.Error("foreign key was wrong value", a.ID, second.BannerID)
		}

		if first.R.Banner != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Banner != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.UserBanners[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.UserBanners[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.UserBanners().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testBannersReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testBannersReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BannerSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testBannersSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Banners().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	bannerDBTypes = map[string]string{`ID`: `integer`, `Name`: `character varying`, `Description`: `character varying`, `Image`: `character varying`, `Points`: `integer`, `Type`: `character varying`, `Status`: `character varying`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`}
	_             = bytes.MinRead
)

func testBannersUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(bannerPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(bannerAllColumns) == len(bannerPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBannersSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(bannerAllColumns) == len(bannerPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Banner{}
	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, bannerDBTypes, true, bannerPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(bannerAllColumns, bannerPrimaryKeyColumns) {
		fields = bannerAllColumns
	} else {
		fields = strmangle.SetComplement(
			bannerAllColumns,
			bannerPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BannerSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBannersUpsert(t *testing.T) {
	t.Parallel()

	if len(bannerAllColumns) == len(bannerPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Banner{}
	if err = randomize.Struct(seed, &o, bannerDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Banner: %s", err)
	}

	count, err := Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, bannerDBTypes, false, bannerPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Banner struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Banner: %s", err)
	}

	count, err = Banners().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
