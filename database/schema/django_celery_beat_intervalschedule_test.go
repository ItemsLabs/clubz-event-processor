// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDjangoCeleryBeatIntervalschedules(t *testing.T) {
	t.Parallel()

	query := DjangoCeleryBeatIntervalschedules()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDjangoCeleryBeatIntervalschedulesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoCeleryBeatIntervalschedulesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DjangoCeleryBeatIntervalschedules().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoCeleryBeatIntervalschedulesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DjangoCeleryBeatIntervalscheduleSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoCeleryBeatIntervalschedulesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DjangoCeleryBeatIntervalscheduleExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if DjangoCeleryBeatIntervalschedule exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DjangoCeleryBeatIntervalscheduleExists to return true, but got false.")
	}
}

func testDjangoCeleryBeatIntervalschedulesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	djangoCeleryBeatIntervalscheduleFound, err := FindDjangoCeleryBeatIntervalschedule(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if djangoCeleryBeatIntervalscheduleFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDjangoCeleryBeatIntervalschedulesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DjangoCeleryBeatIntervalschedules().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testDjangoCeleryBeatIntervalschedulesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DjangoCeleryBeatIntervalschedules().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDjangoCeleryBeatIntervalschedulesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	djangoCeleryBeatIntervalscheduleOne := &DjangoCeleryBeatIntervalschedule{}
	djangoCeleryBeatIntervalscheduleTwo := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, djangoCeleryBeatIntervalscheduleOne, djangoCeleryBeatIntervalscheduleDBTypes, false, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}
	if err = randomize.Struct(seed, djangoCeleryBeatIntervalscheduleTwo, djangoCeleryBeatIntervalscheduleDBTypes, false, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = djangoCeleryBeatIntervalscheduleOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = djangoCeleryBeatIntervalscheduleTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DjangoCeleryBeatIntervalschedules().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDjangoCeleryBeatIntervalschedulesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	djangoCeleryBeatIntervalscheduleOne := &DjangoCeleryBeatIntervalschedule{}
	djangoCeleryBeatIntervalscheduleTwo := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, djangoCeleryBeatIntervalscheduleOne, djangoCeleryBeatIntervalscheduleDBTypes, false, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}
	if err = randomize.Struct(seed, djangoCeleryBeatIntervalscheduleTwo, djangoCeleryBeatIntervalscheduleDBTypes, false, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = djangoCeleryBeatIntervalscheduleOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = djangoCeleryBeatIntervalscheduleTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testDjangoCeleryBeatIntervalschedulesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDjangoCeleryBeatIntervalschedulesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(djangoCeleryBeatIntervalscheduleColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDjangoCeleryBeatIntervalscheduleToManyIntervalDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatIntervalschedule
	var b, c DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, djangoCeleryBeatPeriodictaskDBTypes, false, djangoCeleryBeatPeriodictaskColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, djangoCeleryBeatPeriodictaskDBTypes, false, djangoCeleryBeatPeriodictaskColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.IntervalID, a.ID)
	queries.Assign(&c.IntervalID, a.ID)
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.IntervalDjangoCeleryBeatPeriodictasks().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.IntervalID, b.IntervalID) {
			bFound = true
		}
		if queries.Equal(v.IntervalID, c.IntervalID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DjangoCeleryBeatIntervalscheduleSlice{&a}
	if err = a.L.LoadIntervalDjangoCeleryBeatPeriodictasks(tx, false, (*[]*DjangoCeleryBeatIntervalschedule)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.IntervalDjangoCeleryBeatPeriodictasks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.IntervalDjangoCeleryBeatPeriodictasks = nil
	if err = a.L.LoadIntervalDjangoCeleryBeatPeriodictasks(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.IntervalDjangoCeleryBeatPeriodictasks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDjangoCeleryBeatIntervalscheduleToManyAddOpIntervalDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatIntervalschedule
	var b, c, d, e DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatIntervalscheduleDBTypes, false, strmangle.SetComplement(djangoCeleryBeatIntervalschedulePrimaryKeyColumns, djangoCeleryBeatIntervalscheduleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DjangoCeleryBeatPeriodictask{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, djangoCeleryBeatPeriodictaskDBTypes, false, strmangle.SetComplement(djangoCeleryBeatPeriodictaskPrimaryKeyColumns, djangoCeleryBeatPeriodictaskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DjangoCeleryBeatPeriodictask{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddIntervalDjangoCeleryBeatPeriodictasks(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.IntervalID) {
			t.Error("foreign key was wrong value", a.ID, first.IntervalID)
		}
		if !queries.Equal(a.ID, second.IntervalID) {
			t.Error("foreign key was wrong value", a.ID, second.IntervalID)
		}

		if first.R.Interval != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Interval != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.IntervalDjangoCeleryBeatPeriodictasks[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.IntervalDjangoCeleryBeatPeriodictasks[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.IntervalDjangoCeleryBeatPeriodictasks().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testDjangoCeleryBeatIntervalscheduleToManySetOpIntervalDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatIntervalschedule
	var b, c, d, e DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatIntervalscheduleDBTypes, false, strmangle.SetComplement(djangoCeleryBeatIntervalschedulePrimaryKeyColumns, djangoCeleryBeatIntervalscheduleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DjangoCeleryBeatPeriodictask{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, djangoCeleryBeatPeriodictaskDBTypes, false, strmangle.SetComplement(djangoCeleryBeatPeriodictaskPrimaryKeyColumns, djangoCeleryBeatPeriodictaskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetIntervalDjangoCeleryBeatPeriodictasks(tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.IntervalDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetIntervalDjangoCeleryBeatPeriodictasks(tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.IntervalDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.IntervalID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.IntervalID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.IntervalID) {
		t.Error("foreign key was wrong value", a.ID, d.IntervalID)
	}
	if !queries.Equal(a.ID, e.IntervalID) {
		t.Error("foreign key was wrong value", a.ID, e.IntervalID)
	}

	if b.R.Interval != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Interval != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Interval != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Interval != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.IntervalDjangoCeleryBeatPeriodictasks[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.IntervalDjangoCeleryBeatPeriodictasks[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testDjangoCeleryBeatIntervalscheduleToManyRemoveOpIntervalDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatIntervalschedule
	var b, c, d, e DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatIntervalscheduleDBTypes, false, strmangle.SetComplement(djangoCeleryBeatIntervalschedulePrimaryKeyColumns, djangoCeleryBeatIntervalscheduleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DjangoCeleryBeatPeriodictask{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, djangoCeleryBeatPeriodictaskDBTypes, false, strmangle.SetComplement(djangoCeleryBeatPeriodictaskPrimaryKeyColumns, djangoCeleryBeatPeriodictaskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddIntervalDjangoCeleryBeatPeriodictasks(tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.IntervalDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveIntervalDjangoCeleryBeatPeriodictasks(tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.IntervalDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.IntervalID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.IntervalID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Interval != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Interval != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Interval != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Interval != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.IntervalDjangoCeleryBeatPeriodictasks) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.IntervalDjangoCeleryBeatPeriodictasks[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.IntervalDjangoCeleryBeatPeriodictasks[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testDjangoCeleryBeatIntervalschedulesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testDjangoCeleryBeatIntervalschedulesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DjangoCeleryBeatIntervalscheduleSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testDjangoCeleryBeatIntervalschedulesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DjangoCeleryBeatIntervalschedules().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	djangoCeleryBeatIntervalscheduleDBTypes = map[string]string{`ID`: `integer`, `Every`: `integer`, `Period`: `character varying`}
	_                                       = bytes.MinRead
)

func testDjangoCeleryBeatIntervalschedulesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(djangoCeleryBeatIntervalschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(djangoCeleryBeatIntervalscheduleAllColumns) == len(djangoCeleryBeatIntervalschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalschedulePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDjangoCeleryBeatIntervalschedulesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(djangoCeleryBeatIntervalscheduleAllColumns) == len(djangoCeleryBeatIntervalschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, djangoCeleryBeatIntervalscheduleDBTypes, true, djangoCeleryBeatIntervalschedulePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(djangoCeleryBeatIntervalscheduleAllColumns, djangoCeleryBeatIntervalschedulePrimaryKeyColumns) {
		fields = djangoCeleryBeatIntervalscheduleAllColumns
	} else {
		fields = strmangle.SetComplement(
			djangoCeleryBeatIntervalscheduleAllColumns,
			djangoCeleryBeatIntervalschedulePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DjangoCeleryBeatIntervalscheduleSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDjangoCeleryBeatIntervalschedulesUpsert(t *testing.T) {
	t.Parallel()

	if len(djangoCeleryBeatIntervalscheduleAllColumns) == len(djangoCeleryBeatIntervalschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DjangoCeleryBeatIntervalschedule{}
	if err = randomize.Struct(seed, &o, djangoCeleryBeatIntervalscheduleDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DjangoCeleryBeatIntervalschedule: %s", err)
	}

	count, err := DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, djangoCeleryBeatIntervalscheduleDBTypes, false, djangoCeleryBeatIntervalschedulePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatIntervalschedule struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DjangoCeleryBeatIntervalschedule: %s", err)
	}

	count, err = DjangoCeleryBeatIntervalschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
