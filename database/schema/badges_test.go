// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBadges(t *testing.T) {
	t.Parallel()

	query := Badges()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBadgesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBadgesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Badges().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBadgesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BadgeSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBadgesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BadgeExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Badge exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BadgeExists to return true, but got false.")
	}
}

func testBadgesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	badgeFound, err := FindBadge(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if badgeFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBadgesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Badges().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testBadgesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Badges().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBadgesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	badgeOne := &Badge{}
	badgeTwo := &Badge{}
	if err = randomize.Struct(seed, badgeOne, badgeDBTypes, false, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}
	if err = randomize.Struct(seed, badgeTwo, badgeDBTypes, false, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = badgeOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = badgeTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Badges().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBadgesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	badgeOne := &Badge{}
	badgeTwo := &Badge{}
	if err = randomize.Struct(seed, badgeOne, badgeDBTypes, false, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}
	if err = randomize.Struct(seed, badgeTwo, badgeDBTypes, false, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = badgeOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = badgeTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testBadgesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBadgesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(badgeColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBadgeToManyUserBadges(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Badge
	var b, c UserBadge

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.BadgeID = a.ID
	c.BadgeID = a.ID

	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.UserBadges().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.BadgeID == b.BadgeID {
			bFound = true
		}
		if v.BadgeID == c.BadgeID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := BadgeSlice{&a}
	if err = a.L.LoadUserBadges(tx, false, (*[]*Badge)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserBadges); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.UserBadges = nil
	if err = a.L.LoadUserBadges(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserBadges); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testBadgeToManyAddOpUserBadges(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Badge
	var b, c, d, e UserBadge

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, badgeDBTypes, false, strmangle.SetComplement(badgePrimaryKeyColumns, badgeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*UserBadge{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, userBadgeDBTypes, false, strmangle.SetComplement(userBadgePrimaryKeyColumns, userBadgeColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*UserBadge{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddUserBadges(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.BadgeID {
			t.Error("foreign key was wrong value", a.ID, first.BadgeID)
		}
		if a.ID != second.BadgeID {
			t.Error("foreign key was wrong value", a.ID, second.BadgeID)
		}

		if first.R.Badge != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Badge != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.UserBadges[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.UserBadges[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.UserBadges().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testBadgesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testBadgesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BadgeSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testBadgesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Badges().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	badgeDBTypes = map[string]string{`ID`: `integer`, `Name`: `character varying`, `Description`: `character varying`, `Image`: `character varying`, `Points`: `integer`, `Type`: `character varying`, `Status`: `character varying`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`}
	_            = bytes.MinRead
)

func testBadgesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(badgePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(badgeAllColumns) == len(badgePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBadgesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(badgeAllColumns) == len(badgePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Badge{}
	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, badgeDBTypes, true, badgePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(badgeAllColumns, badgePrimaryKeyColumns) {
		fields = badgeAllColumns
	} else {
		fields = strmangle.SetComplement(
			badgeAllColumns,
			badgePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BadgeSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBadgesUpsert(t *testing.T) {
	t.Parallel()

	if len(badgeAllColumns) == len(badgePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Badge{}
	if err = randomize.Struct(seed, &o, badgeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Badge: %s", err)
	}

	count, err := Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, badgeDBTypes, false, badgePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Badge: %s", err)
	}

	count, err = Badges().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
