// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSelectionTeamPlayers(t *testing.T) {
	t.Parallel()

	query := SelectionTeamPlayers()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSelectionTeamPlayersDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSelectionTeamPlayersQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SelectionTeamPlayers().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSelectionTeamPlayersSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SelectionTeamPlayerSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSelectionTeamPlayersExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SelectionTeamPlayerExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SelectionTeamPlayer exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SelectionTeamPlayerExists to return true, but got false.")
	}
}

func testSelectionTeamPlayersFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	selectionTeamPlayerFound, err := FindSelectionTeamPlayer(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if selectionTeamPlayerFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSelectionTeamPlayersBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SelectionTeamPlayers().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testSelectionTeamPlayersOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SelectionTeamPlayers().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSelectionTeamPlayersAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	selectionTeamPlayerOne := &SelectionTeamPlayer{}
	selectionTeamPlayerTwo := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, selectionTeamPlayerOne, selectionTeamPlayerDBTypes, false, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}
	if err = randomize.Struct(seed, selectionTeamPlayerTwo, selectionTeamPlayerDBTypes, false, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = selectionTeamPlayerOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = selectionTeamPlayerTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SelectionTeamPlayers().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSelectionTeamPlayersCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	selectionTeamPlayerOne := &SelectionTeamPlayer{}
	selectionTeamPlayerTwo := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, selectionTeamPlayerOne, selectionTeamPlayerDBTypes, false, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}
	if err = randomize.Struct(seed, selectionTeamPlayerTwo, selectionTeamPlayerDBTypes, false, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = selectionTeamPlayerOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = selectionTeamPlayerTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSelectionTeamPlayersInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSelectionTeamPlayersInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(selectionTeamPlayerColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSelectionTeamPlayerToOnePlayerUsingPlayer(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local SelectionTeamPlayer
	var foreign Player

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, selectionTeamPlayerDBTypes, false, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, playerDBTypes, false, playerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Player struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.PlayerID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Player().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SelectionTeamPlayerSlice{&local}
	if err = local.L.LoadPlayer(tx, false, (*[]*SelectionTeamPlayer)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Player == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Player = nil
	if err = local.L.LoadPlayer(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Player == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testSelectionTeamPlayerToOneSetOpPlayerUsingPlayer(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a SelectionTeamPlayer
	var b, c Player

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, selectionTeamPlayerDBTypes, false, strmangle.SetComplement(selectionTeamPlayerPrimaryKeyColumns, selectionTeamPlayerColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, playerDBTypes, false, strmangle.SetComplement(playerPrimaryKeyColumns, playerColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, playerDBTypes, false, strmangle.SetComplement(playerPrimaryKeyColumns, playerColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Player{&b, &c} {
		err = a.SetPlayer(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Player != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.SelectionTeamPlayers[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.PlayerID != x.ID {
			t.Error("foreign key was wrong value", a.PlayerID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.PlayerID))
		reflect.Indirect(reflect.ValueOf(&a.PlayerID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.PlayerID != x.ID {
			t.Error("foreign key was wrong value", a.PlayerID, x.ID)
		}
	}
}

func testSelectionTeamPlayersReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testSelectionTeamPlayersReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SelectionTeamPlayerSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testSelectionTeamPlayersSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SelectionTeamPlayers().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	selectionTeamPlayerDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `SelectionID`: `character varying`, `Position`: `character varying`, `JerseyNumber`: `integer`, `PlayerID`: `uuid`}
	_                          = bytes.MinRead
)

func testSelectionTeamPlayersUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(selectionTeamPlayerPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(selectionTeamPlayerAllColumns) == len(selectionTeamPlayerPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSelectionTeamPlayersSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(selectionTeamPlayerAllColumns) == len(selectionTeamPlayerPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SelectionTeamPlayer{}
	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, selectionTeamPlayerDBTypes, true, selectionTeamPlayerPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(selectionTeamPlayerAllColumns, selectionTeamPlayerPrimaryKeyColumns) {
		fields = selectionTeamPlayerAllColumns
	} else {
		fields = strmangle.SetComplement(
			selectionTeamPlayerAllColumns,
			selectionTeamPlayerPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SelectionTeamPlayerSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSelectionTeamPlayersUpsert(t *testing.T) {
	t.Parallel()

	if len(selectionTeamPlayerAllColumns) == len(selectionTeamPlayerPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SelectionTeamPlayer{}
	if err = randomize.Struct(seed, &o, selectionTeamPlayerDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SelectionTeamPlayer: %s", err)
	}

	count, err := SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, selectionTeamPlayerDBTypes, false, selectionTeamPlayerPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SelectionTeamPlayer struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SelectionTeamPlayer: %s", err)
	}

	count, err = SelectionTeamPlayers().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
