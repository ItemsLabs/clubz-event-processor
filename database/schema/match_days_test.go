// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testMatchDays(t *testing.T) {
	t.Parallel()

	query := MatchDays()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testMatchDaysDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchDaysQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := MatchDays().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchDaysSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MatchDaySlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchDaysExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := MatchDayExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if MatchDay exists: %s", err)
	}
	if !e {
		t.Errorf("Expected MatchDayExists to return true, but got false.")
	}
}

func testMatchDaysFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	matchDayFound, err := FindMatchDay(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if matchDayFound == nil {
		t.Error("want a record, got nil")
	}
}

func testMatchDaysBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = MatchDays().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testMatchDaysOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := MatchDays().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testMatchDaysAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	matchDayOne := &MatchDay{}
	matchDayTwo := &MatchDay{}
	if err = randomize.Struct(seed, matchDayOne, matchDayDBTypes, false, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}
	if err = randomize.Struct(seed, matchDayTwo, matchDayDBTypes, false, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = matchDayOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = matchDayTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MatchDays().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testMatchDaysCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	matchDayOne := &MatchDay{}
	matchDayTwo := &MatchDay{}
	if err = randomize.Struct(seed, matchDayOne, matchDayDBTypes, false, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}
	if err = randomize.Struct(seed, matchDayTwo, matchDayDBTypes, false, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = matchDayOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = matchDayTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testMatchDaysInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMatchDaysInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(matchDayColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMatchDayToManyMatches(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchDay
	var b, c Match

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, matchDBTypes, false, matchColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, matchDBTypes, false, matchColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.MatchDayID, a.ID)
	queries.Assign(&c.MatchDayID, a.ID)
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.Matches().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.MatchDayID, b.MatchDayID) {
			bFound = true
		}
		if queries.Equal(v.MatchDayID, c.MatchDayID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := MatchDaySlice{&a}
	if err = a.L.LoadMatches(tx, false, (*[]*MatchDay)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Matches); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.Matches = nil
	if err = a.L.LoadMatches(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.Matches); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testMatchDayToManyAddOpMatches(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchDay
	var b, c, d, e Match

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchDayDBTypes, false, strmangle.SetComplement(matchDayPrimaryKeyColumns, matchDayColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Match{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*Match{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddMatches(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.MatchDayID) {
			t.Error("foreign key was wrong value", a.ID, first.MatchDayID)
		}
		if !queries.Equal(a.ID, second.MatchDayID) {
			t.Error("foreign key was wrong value", a.ID, second.MatchDayID)
		}

		if first.R.MatchDay != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.MatchDay != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.Matches[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.Matches[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.Matches().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testMatchDayToManySetOpMatches(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchDay
	var b, c, d, e Match

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchDayDBTypes, false, strmangle.SetComplement(matchDayPrimaryKeyColumns, matchDayColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Match{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetMatches(tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.Matches().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetMatches(tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.Matches().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.MatchDayID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.MatchDayID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.MatchDayID) {
		t.Error("foreign key was wrong value", a.ID, d.MatchDayID)
	}
	if !queries.Equal(a.ID, e.MatchDayID) {
		t.Error("foreign key was wrong value", a.ID, e.MatchDayID)
	}

	if b.R.MatchDay != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.MatchDay != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.MatchDay != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.MatchDay != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.Matches[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.Matches[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testMatchDayToManyRemoveOpMatches(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchDay
	var b, c, d, e Match

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchDayDBTypes, false, strmangle.SetComplement(matchDayPrimaryKeyColumns, matchDayColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Match{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddMatches(tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.Matches().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveMatches(tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.Matches().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.MatchDayID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.MatchDayID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.MatchDay != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.MatchDay != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.MatchDay != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.MatchDay != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.Matches) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.Matches[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.Matches[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testMatchDaysReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testMatchDaysReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MatchDaySlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testMatchDaysSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MatchDays().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	matchDayDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `Name`: `character varying`, `Description`: `text`, `StartDate`: `timestamp with time zone`, `EndDate`: `timestamp with time zone`}
	_               = bytes.MinRead
)

func testMatchDaysUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(matchDayPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(matchDayAllColumns) == len(matchDayPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testMatchDaysSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(matchDayAllColumns) == len(matchDayPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MatchDay{}
	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, matchDayDBTypes, true, matchDayPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(matchDayAllColumns, matchDayPrimaryKeyColumns) {
		fields = matchDayAllColumns
	} else {
		fields = strmangle.SetComplement(
			matchDayAllColumns,
			matchDayPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := MatchDaySlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testMatchDaysUpsert(t *testing.T) {
	t.Parallel()

	if len(matchDayAllColumns) == len(matchDayPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := MatchDay{}
	if err = randomize.Struct(seed, &o, matchDayDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MatchDay: %s", err)
	}

	count, err := MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, matchDayDBTypes, false, matchDayPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchDay struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MatchDay: %s", err)
	}

	count, err = MatchDays().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
