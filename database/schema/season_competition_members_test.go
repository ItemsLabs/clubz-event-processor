// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSeasonCompetitionMembers(t *testing.T) {
	t.Parallel()

	query := SeasonCompetitionMembers()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSeasonCompetitionMembersDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSeasonCompetitionMembersQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SeasonCompetitionMembers().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSeasonCompetitionMembersSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SeasonCompetitionMemberSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSeasonCompetitionMembersExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SeasonCompetitionMemberExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SeasonCompetitionMember exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SeasonCompetitionMemberExists to return true, but got false.")
	}
}

func testSeasonCompetitionMembersFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	seasonCompetitionMemberFound, err := FindSeasonCompetitionMember(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if seasonCompetitionMemberFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSeasonCompetitionMembersBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SeasonCompetitionMembers().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testSeasonCompetitionMembersOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SeasonCompetitionMembers().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSeasonCompetitionMembersAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	seasonCompetitionMemberOne := &SeasonCompetitionMember{}
	seasonCompetitionMemberTwo := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, seasonCompetitionMemberOne, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}
	if err = randomize.Struct(seed, seasonCompetitionMemberTwo, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = seasonCompetitionMemberOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = seasonCompetitionMemberTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SeasonCompetitionMembers().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSeasonCompetitionMembersCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	seasonCompetitionMemberOne := &SeasonCompetitionMember{}
	seasonCompetitionMemberTwo := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, seasonCompetitionMemberOne, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}
	if err = randomize.Struct(seed, seasonCompetitionMemberTwo, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = seasonCompetitionMemberOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = seasonCompetitionMemberTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSeasonCompetitionMembersInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSeasonCompetitionMembersInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(seasonCompetitionMemberColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSeasonCompetitionMemberToOneCompetitionUsingCompetition(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local SeasonCompetitionMember
	var foreign Competition

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, competitionDBTypes, false, competitionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Competition struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.CompetitionID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Competition().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SeasonCompetitionMemberSlice{&local}
	if err = local.L.LoadCompetition(tx, false, (*[]*SeasonCompetitionMember)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Competition == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Competition = nil
	if err = local.L.LoadCompetition(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Competition == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testSeasonCompetitionMemberToOneSeasonUsingSeason(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local SeasonCompetitionMember
	var foreign Season

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, seasonDBTypes, false, seasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Season struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.SeasonID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Season().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SeasonCompetitionMemberSlice{&local}
	if err = local.L.LoadSeason(tx, false, (*[]*SeasonCompetitionMember)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Season == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Season = nil
	if err = local.L.LoadSeason(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Season == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testSeasonCompetitionMemberToOneTeamUsingTeam(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local SeasonCompetitionMember
	var foreign Team

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, teamDBTypes, false, teamColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Team struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.TeamID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Team().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := SeasonCompetitionMemberSlice{&local}
	if err = local.L.LoadTeam(tx, false, (*[]*SeasonCompetitionMember)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Team == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Team = nil
	if err = local.L.LoadTeam(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Team == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testSeasonCompetitionMemberToOneSetOpCompetitionUsingCompetition(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a SeasonCompetitionMember
	var b, c Competition

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, seasonCompetitionMemberDBTypes, false, strmangle.SetComplement(seasonCompetitionMemberPrimaryKeyColumns, seasonCompetitionMemberColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, competitionDBTypes, false, strmangle.SetComplement(competitionPrimaryKeyColumns, competitionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, competitionDBTypes, false, strmangle.SetComplement(competitionPrimaryKeyColumns, competitionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Competition{&b, &c} {
		err = a.SetCompetition(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Competition != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.SeasonCompetitionMembers[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.CompetitionID != x.ID {
			t.Error("foreign key was wrong value", a.CompetitionID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.CompetitionID))
		reflect.Indirect(reflect.ValueOf(&a.CompetitionID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.CompetitionID != x.ID {
			t.Error("foreign key was wrong value", a.CompetitionID, x.ID)
		}
	}
}
func testSeasonCompetitionMemberToOneSetOpSeasonUsingSeason(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a SeasonCompetitionMember
	var b, c Season

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, seasonCompetitionMemberDBTypes, false, strmangle.SetComplement(seasonCompetitionMemberPrimaryKeyColumns, seasonCompetitionMemberColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, seasonDBTypes, false, strmangle.SetComplement(seasonPrimaryKeyColumns, seasonColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, seasonDBTypes, false, strmangle.SetComplement(seasonPrimaryKeyColumns, seasonColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Season{&b, &c} {
		err = a.SetSeason(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Season != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.SeasonCompetitionMembers[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.SeasonID != x.ID {
			t.Error("foreign key was wrong value", a.SeasonID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.SeasonID))
		reflect.Indirect(reflect.ValueOf(&a.SeasonID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.SeasonID != x.ID {
			t.Error("foreign key was wrong value", a.SeasonID, x.ID)
		}
	}
}
func testSeasonCompetitionMemberToOneSetOpTeamUsingTeam(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a SeasonCompetitionMember
	var b, c Team

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, seasonCompetitionMemberDBTypes, false, strmangle.SetComplement(seasonCompetitionMemberPrimaryKeyColumns, seasonCompetitionMemberColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, teamDBTypes, false, strmangle.SetComplement(teamPrimaryKeyColumns, teamColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, teamDBTypes, false, strmangle.SetComplement(teamPrimaryKeyColumns, teamColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Team{&b, &c} {
		err = a.SetTeam(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Team != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.SeasonCompetitionMembers[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.TeamID != x.ID {
			t.Error("foreign key was wrong value", a.TeamID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.TeamID))
		reflect.Indirect(reflect.ValueOf(&a.TeamID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.TeamID != x.ID {
			t.Error("foreign key was wrong value", a.TeamID, x.ID)
		}
	}
}

func testSeasonCompetitionMembersReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testSeasonCompetitionMembersReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SeasonCompetitionMemberSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testSeasonCompetitionMembersSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SeasonCompetitionMembers().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	seasonCompetitionMemberDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `CompetitionID`: `uuid`, `SeasonID`: `uuid`, `TeamID`: `uuid`}
	_                              = bytes.MinRead
)

func testSeasonCompetitionMembersUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(seasonCompetitionMemberPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(seasonCompetitionMemberAllColumns) == len(seasonCompetitionMemberPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSeasonCompetitionMembersSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(seasonCompetitionMemberAllColumns) == len(seasonCompetitionMemberPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SeasonCompetitionMember{}
	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, seasonCompetitionMemberDBTypes, true, seasonCompetitionMemberPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(seasonCompetitionMemberAllColumns, seasonCompetitionMemberPrimaryKeyColumns) {
		fields = seasonCompetitionMemberAllColumns
	} else {
		fields = strmangle.SetComplement(
			seasonCompetitionMemberAllColumns,
			seasonCompetitionMemberPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SeasonCompetitionMemberSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSeasonCompetitionMembersUpsert(t *testing.T) {
	t.Parallel()

	if len(seasonCompetitionMemberAllColumns) == len(seasonCompetitionMemberPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SeasonCompetitionMember{}
	if err = randomize.Struct(seed, &o, seasonCompetitionMemberDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SeasonCompetitionMember: %s", err)
	}

	count, err := SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, seasonCompetitionMemberDBTypes, false, seasonCompetitionMemberPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SeasonCompetitionMember struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SeasonCompetitionMember: %s", err)
	}

	count, err = SeasonCompetitionMembers().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
