// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testActions(t *testing.T) {
	t.Parallel()

	query := Actions()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testActionsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testActionsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Actions().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testActionsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ActionSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testActionsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ActionExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Action exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ActionExists to return true, but got false.")
	}
}

func testActionsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	actionFound, err := FindAction(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if actionFound == nil {
		t.Error("want a record, got nil")
	}
}

func testActionsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Actions().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testActionsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Actions().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testActionsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	actionOne := &Action{}
	actionTwo := &Action{}
	if err = randomize.Struct(seed, actionOne, actionDBTypes, false, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}
	if err = randomize.Struct(seed, actionTwo, actionDBTypes, false, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = actionOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = actionTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Actions().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testActionsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	actionOne := &Action{}
	actionTwo := &Action{}
	if err = randomize.Struct(seed, actionOne, actionDBTypes, false, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}
	if err = randomize.Struct(seed, actionTwo, actionDBTypes, false, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = actionOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = actionTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testActionsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testActionsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(actionColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testActionToManyPowerupActions(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Action
	var b, c PowerupAction

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, powerupActionDBTypes, false, powerupActionColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, powerupActionDBTypes, false, powerupActionColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.ActionID = a.ID
	c.ActionID = a.ID

	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.PowerupActions().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.ActionID == b.ActionID {
			bFound = true
		}
		if v.ActionID == c.ActionID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := ActionSlice{&a}
	if err = a.L.LoadPowerupActions(tx, false, (*[]*Action)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.PowerupActions); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.PowerupActions = nil
	if err = a.L.LoadPowerupActions(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.PowerupActions); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testActionToManyAddOpPowerupActions(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Action
	var b, c, d, e PowerupAction

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, actionDBTypes, false, strmangle.SetComplement(actionPrimaryKeyColumns, actionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*PowerupAction{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, powerupActionDBTypes, false, strmangle.SetComplement(powerupActionPrimaryKeyColumns, powerupActionColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*PowerupAction{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddPowerupActions(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.ActionID {
			t.Error("foreign key was wrong value", a.ID, first.ActionID)
		}
		if a.ID != second.ActionID {
			t.Error("foreign key was wrong value", a.ID, second.ActionID)
		}

		if first.R.Action != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Action != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.PowerupActions[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.PowerupActions[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.PowerupActions().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testActionToOneSportUsingSport(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local Action
	var foreign Sport

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, sportDBTypes, false, sportColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Sport struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.SportID, foreign.ID)
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Sport().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := ActionSlice{&local}
	if err = local.L.LoadSport(tx, false, (*[]*Action)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Sport == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Sport = nil
	if err = local.L.LoadSport(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Sport == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testActionToOneSetOpSportUsingSport(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Action
	var b, c Sport

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, actionDBTypes, false, strmangle.SetComplement(actionPrimaryKeyColumns, actionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, sportDBTypes, false, strmangle.SetComplement(sportPrimaryKeyColumns, sportColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, sportDBTypes, false, strmangle.SetComplement(sportPrimaryKeyColumns, sportColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Sport{&b, &c} {
		err = a.SetSport(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Sport != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Actions[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.SportID, x.ID) {
			t.Error("foreign key was wrong value", a.SportID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.SportID))
		reflect.Indirect(reflect.ValueOf(&a.SportID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.SportID, x.ID) {
			t.Error("foreign key was wrong value", a.SportID, x.ID)
		}
	}
}

func testActionToOneRemoveOpSportUsingSport(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a Action
	var b Sport

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, actionDBTypes, false, strmangle.SetComplement(actionPrimaryKeyColumns, actionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, sportDBTypes, false, strmangle.SetComplement(sportPrimaryKeyColumns, sportColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetSport(tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveSport(tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Sport().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Sport != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.SportID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.Actions) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testActionsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testActionsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ActionSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testActionsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Actions().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	actionDBTypes = map[string]string{`ID`: `integer`, `Name`: `text`, `Description`: `text`, `Score`: `double precision`, `Ordering`: `integer`, `SportID`: `uuid`, `Category`: `text`, `Icon`: `text`, `NFTCategory`: `text`}
	_             = bytes.MinRead
)

func testActionsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(actionPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(actionAllColumns) == len(actionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, actionDBTypes, true, actionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testActionsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(actionAllColumns) == len(actionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Action{}
	if err = randomize.Struct(seed, o, actionDBTypes, true, actionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, actionDBTypes, true, actionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(actionAllColumns, actionPrimaryKeyColumns) {
		fields = actionAllColumns
	} else {
		fields = strmangle.SetComplement(
			actionAllColumns,
			actionPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ActionSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testActionsUpsert(t *testing.T) {
	t.Parallel()

	if len(actionAllColumns) == len(actionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Action{}
	if err = randomize.Struct(seed, &o, actionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Action: %s", err)
	}

	count, err := Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, actionDBTypes, false, actionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Action struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Action: %s", err)
	}

	count, err = Actions().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
