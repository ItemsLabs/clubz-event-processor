// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserBadges(t *testing.T) {
	t.Parallel()

	query := UserBadges()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserBadgesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserBadgesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserBadges().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserBadgesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserBadgeSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserBadgesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserBadgeExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserBadge exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserBadgeExists to return true, but got false.")
	}
}

func testUserBadgesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userBadgeFound, err := FindUserBadge(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userBadgeFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserBadgesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserBadges().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserBadgesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserBadges().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserBadgesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userBadgeOne := &UserBadge{}
	userBadgeTwo := &UserBadge{}
	if err = randomize.Struct(seed, userBadgeOne, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}
	if err = randomize.Struct(seed, userBadgeTwo, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = userBadgeOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userBadgeTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserBadges().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserBadgesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userBadgeOne := &UserBadge{}
	userBadgeTwo := &UserBadge{}
	if err = randomize.Struct(seed, userBadgeOne, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}
	if err = randomize.Struct(seed, userBadgeTwo, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = userBadgeOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userBadgeTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testUserBadgesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserBadgesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(userBadgeColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserBadgeToOneBadgeUsingBadge(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local UserBadge
	var foreign Badge

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, badgeDBTypes, false, badgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Badge struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.BadgeID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Badge().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserBadgeSlice{&local}
	if err = local.L.LoadBadge(tx, false, (*[]*UserBadge)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Badge == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Badge = nil
	if err = local.L.LoadBadge(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Badge == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testUserBadgeToOneUserUsingUser(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local UserBadge
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userBadgeDBTypes, false, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserBadgeSlice{&local}
	if err = local.L.LoadUser(tx, false, (*[]*UserBadge)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testUserBadgeToOneSetOpBadgeUsingBadge(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserBadge
	var b, c Badge

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userBadgeDBTypes, false, strmangle.SetComplement(userBadgePrimaryKeyColumns, userBadgeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, badgeDBTypes, false, strmangle.SetComplement(badgePrimaryKeyColumns, badgeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, badgeDBTypes, false, strmangle.SetComplement(badgePrimaryKeyColumns, badgeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Badge{&b, &c} {
		err = a.SetBadge(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Badge != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserBadges[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.BadgeID != x.ID {
			t.Error("foreign key was wrong value", a.BadgeID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.BadgeID))
		reflect.Indirect(reflect.ValueOf(&a.BadgeID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.BadgeID != x.ID {
			t.Error("foreign key was wrong value", a.BadgeID, x.ID)
		}
	}
}
func testUserBadgeToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserBadge
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userBadgeDBTypes, false, strmangle.SetComplement(userBadgePrimaryKeyColumns, userBadgeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserBadges[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testUserBadgesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testUserBadgesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserBadgeSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testUserBadgesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserBadges().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userBadgeDBTypes = map[string]string{`ID`: `integer`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `BadgeID`: `integer`, `UserID`: `uuid`}
	_                = bytes.MinRead
)

func testUserBadgesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userBadgePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userBadgeAllColumns) == len(userBadgePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserBadgesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userBadgeAllColumns) == len(userBadgePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserBadge{}
	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userBadgeDBTypes, true, userBadgePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userBadgeAllColumns, userBadgePrimaryKeyColumns) {
		fields = userBadgeAllColumns
	} else {
		fields = strmangle.SetComplement(
			userBadgeAllColumns,
			userBadgePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserBadgeSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserBadgesUpsert(t *testing.T) {
	t.Parallel()

	if len(userBadgeAllColumns) == len(userBadgePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserBadge{}
	if err = randomize.Struct(seed, &o, userBadgeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserBadge: %s", err)
	}

	count, err := UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userBadgeDBTypes, false, userBadgePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserBadge struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserBadge: %s", err)
	}

	count, err = UserBadges().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
