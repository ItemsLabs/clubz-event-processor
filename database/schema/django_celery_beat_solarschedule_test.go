// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDjangoCeleryBeatSolarschedules(t *testing.T) {
	t.Parallel()

	query := DjangoCeleryBeatSolarschedules()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDjangoCeleryBeatSolarschedulesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoCeleryBeatSolarschedulesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DjangoCeleryBeatSolarschedules().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoCeleryBeatSolarschedulesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DjangoCeleryBeatSolarscheduleSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoCeleryBeatSolarschedulesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DjangoCeleryBeatSolarscheduleExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if DjangoCeleryBeatSolarschedule exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DjangoCeleryBeatSolarscheduleExists to return true, but got false.")
	}
}

func testDjangoCeleryBeatSolarschedulesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	djangoCeleryBeatSolarscheduleFound, err := FindDjangoCeleryBeatSolarschedule(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if djangoCeleryBeatSolarscheduleFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDjangoCeleryBeatSolarschedulesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DjangoCeleryBeatSolarschedules().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testDjangoCeleryBeatSolarschedulesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DjangoCeleryBeatSolarschedules().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDjangoCeleryBeatSolarschedulesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	djangoCeleryBeatSolarscheduleOne := &DjangoCeleryBeatSolarschedule{}
	djangoCeleryBeatSolarscheduleTwo := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, djangoCeleryBeatSolarscheduleOne, djangoCeleryBeatSolarscheduleDBTypes, false, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}
	if err = randomize.Struct(seed, djangoCeleryBeatSolarscheduleTwo, djangoCeleryBeatSolarscheduleDBTypes, false, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = djangoCeleryBeatSolarscheduleOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = djangoCeleryBeatSolarscheduleTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DjangoCeleryBeatSolarschedules().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDjangoCeleryBeatSolarschedulesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	djangoCeleryBeatSolarscheduleOne := &DjangoCeleryBeatSolarschedule{}
	djangoCeleryBeatSolarscheduleTwo := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, djangoCeleryBeatSolarscheduleOne, djangoCeleryBeatSolarscheduleDBTypes, false, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}
	if err = randomize.Struct(seed, djangoCeleryBeatSolarscheduleTwo, djangoCeleryBeatSolarscheduleDBTypes, false, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = djangoCeleryBeatSolarscheduleOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = djangoCeleryBeatSolarscheduleTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testDjangoCeleryBeatSolarschedulesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDjangoCeleryBeatSolarschedulesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(djangoCeleryBeatSolarscheduleColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDjangoCeleryBeatSolarscheduleToManySolarDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatSolarschedule
	var b, c DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, djangoCeleryBeatPeriodictaskDBTypes, false, djangoCeleryBeatPeriodictaskColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, djangoCeleryBeatPeriodictaskDBTypes, false, djangoCeleryBeatPeriodictaskColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.SolarID, a.ID)
	queries.Assign(&c.SolarID, a.ID)
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.SolarDjangoCeleryBeatPeriodictasks().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.SolarID, b.SolarID) {
			bFound = true
		}
		if queries.Equal(v.SolarID, c.SolarID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DjangoCeleryBeatSolarscheduleSlice{&a}
	if err = a.L.LoadSolarDjangoCeleryBeatPeriodictasks(tx, false, (*[]*DjangoCeleryBeatSolarschedule)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.SolarDjangoCeleryBeatPeriodictasks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.SolarDjangoCeleryBeatPeriodictasks = nil
	if err = a.L.LoadSolarDjangoCeleryBeatPeriodictasks(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.SolarDjangoCeleryBeatPeriodictasks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDjangoCeleryBeatSolarscheduleToManyAddOpSolarDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatSolarschedule
	var b, c, d, e DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatSolarscheduleDBTypes, false, strmangle.SetComplement(djangoCeleryBeatSolarschedulePrimaryKeyColumns, djangoCeleryBeatSolarscheduleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DjangoCeleryBeatPeriodictask{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, djangoCeleryBeatPeriodictaskDBTypes, false, strmangle.SetComplement(djangoCeleryBeatPeriodictaskPrimaryKeyColumns, djangoCeleryBeatPeriodictaskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DjangoCeleryBeatPeriodictask{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddSolarDjangoCeleryBeatPeriodictasks(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.SolarID) {
			t.Error("foreign key was wrong value", a.ID, first.SolarID)
		}
		if !queries.Equal(a.ID, second.SolarID) {
			t.Error("foreign key was wrong value", a.ID, second.SolarID)
		}

		if first.R.Solar != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Solar != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.SolarDjangoCeleryBeatPeriodictasks[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.SolarDjangoCeleryBeatPeriodictasks[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.SolarDjangoCeleryBeatPeriodictasks().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testDjangoCeleryBeatSolarscheduleToManySetOpSolarDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatSolarschedule
	var b, c, d, e DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatSolarscheduleDBTypes, false, strmangle.SetComplement(djangoCeleryBeatSolarschedulePrimaryKeyColumns, djangoCeleryBeatSolarscheduleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DjangoCeleryBeatPeriodictask{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, djangoCeleryBeatPeriodictaskDBTypes, false, strmangle.SetComplement(djangoCeleryBeatPeriodictaskPrimaryKeyColumns, djangoCeleryBeatPeriodictaskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetSolarDjangoCeleryBeatPeriodictasks(tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.SolarDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetSolarDjangoCeleryBeatPeriodictasks(tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.SolarDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.SolarID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.SolarID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.SolarID) {
		t.Error("foreign key was wrong value", a.ID, d.SolarID)
	}
	if !queries.Equal(a.ID, e.SolarID) {
		t.Error("foreign key was wrong value", a.ID, e.SolarID)
	}

	if b.R.Solar != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Solar != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Solar != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Solar != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.SolarDjangoCeleryBeatPeriodictasks[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.SolarDjangoCeleryBeatPeriodictasks[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testDjangoCeleryBeatSolarscheduleToManyRemoveOpSolarDjangoCeleryBeatPeriodictasks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a DjangoCeleryBeatSolarschedule
	var b, c, d, e DjangoCeleryBeatPeriodictask

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, djangoCeleryBeatSolarscheduleDBTypes, false, strmangle.SetComplement(djangoCeleryBeatSolarschedulePrimaryKeyColumns, djangoCeleryBeatSolarscheduleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DjangoCeleryBeatPeriodictask{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, djangoCeleryBeatPeriodictaskDBTypes, false, strmangle.SetComplement(djangoCeleryBeatPeriodictaskPrimaryKeyColumns, djangoCeleryBeatPeriodictaskColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddSolarDjangoCeleryBeatPeriodictasks(tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.SolarDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveSolarDjangoCeleryBeatPeriodictasks(tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.SolarDjangoCeleryBeatPeriodictasks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.SolarID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.SolarID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Solar != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Solar != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Solar != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Solar != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.SolarDjangoCeleryBeatPeriodictasks) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.SolarDjangoCeleryBeatPeriodictasks[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.SolarDjangoCeleryBeatPeriodictasks[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testDjangoCeleryBeatSolarschedulesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testDjangoCeleryBeatSolarschedulesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DjangoCeleryBeatSolarscheduleSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testDjangoCeleryBeatSolarschedulesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DjangoCeleryBeatSolarschedules().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	djangoCeleryBeatSolarscheduleDBTypes = map[string]string{`ID`: `integer`, `Event`: `character varying`, `Latitude`: `numeric`, `Longitude`: `numeric`}
	_                                    = bytes.MinRead
)

func testDjangoCeleryBeatSolarschedulesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(djangoCeleryBeatSolarschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(djangoCeleryBeatSolarscheduleAllColumns) == len(djangoCeleryBeatSolarschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarschedulePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDjangoCeleryBeatSolarschedulesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(djangoCeleryBeatSolarscheduleAllColumns) == len(djangoCeleryBeatSolarschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarscheduleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, djangoCeleryBeatSolarscheduleDBTypes, true, djangoCeleryBeatSolarschedulePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(djangoCeleryBeatSolarscheduleAllColumns, djangoCeleryBeatSolarschedulePrimaryKeyColumns) {
		fields = djangoCeleryBeatSolarscheduleAllColumns
	} else {
		fields = strmangle.SetComplement(
			djangoCeleryBeatSolarscheduleAllColumns,
			djangoCeleryBeatSolarschedulePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DjangoCeleryBeatSolarscheduleSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDjangoCeleryBeatSolarschedulesUpsert(t *testing.T) {
	t.Parallel()

	if len(djangoCeleryBeatSolarscheduleAllColumns) == len(djangoCeleryBeatSolarschedulePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DjangoCeleryBeatSolarschedule{}
	if err = randomize.Struct(seed, &o, djangoCeleryBeatSolarscheduleDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DjangoCeleryBeatSolarschedule: %s", err)
	}

	count, err := DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, djangoCeleryBeatSolarscheduleDBTypes, false, djangoCeleryBeatSolarschedulePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoCeleryBeatSolarschedule struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DjangoCeleryBeatSolarschedule: %s", err)
	}

	count, err = DjangoCeleryBeatSolarschedules().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
