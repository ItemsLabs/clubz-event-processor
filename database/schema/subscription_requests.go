// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SubscriptionRequest is an object representing the database table.
type SubscriptionRequest struct {
	ID                int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID            string    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	LastBillingUpdate time.Time `boil:"last_billing_update" json:"last_billing_update" toml:"last_billing_update" yaml:"last_billing_update"`
	AppUserID         string    `boil:"app_user_id" json:"app_user_id" toml:"app_user_id" yaml:"app_user_id"`

	R *subscriptionRequestR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L subscriptionRequestL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SubscriptionRequestColumns = struct {
	ID                string
	UserID            string
	LastBillingUpdate string
	AppUserID         string
}{
	ID:                "id",
	UserID:            "user_id",
	LastBillingUpdate: "last_billing_update",
	AppUserID:         "app_user_id",
}

var SubscriptionRequestTableColumns = struct {
	ID                string
	UserID            string
	LastBillingUpdate string
	AppUserID         string
}{
	ID:                "subscription_requests.id",
	UserID:            "subscription_requests.user_id",
	LastBillingUpdate: "subscription_requests.last_billing_update",
	AppUserID:         "subscription_requests.app_user_id",
}

// Generated where

var SubscriptionRequestWhere = struct {
	ID                whereHelperint
	UserID            whereHelperstring
	LastBillingUpdate whereHelpertime_Time
	AppUserID         whereHelperstring
}{
	ID:                whereHelperint{field: "\"subscription_requests\".\"id\""},
	UserID:            whereHelperstring{field: "\"subscription_requests\".\"user_id\""},
	LastBillingUpdate: whereHelpertime_Time{field: "\"subscription_requests\".\"last_billing_update\""},
	AppUserID:         whereHelperstring{field: "\"subscription_requests\".\"app_user_id\""},
}

// SubscriptionRequestRels is where relationship names are stored.
var SubscriptionRequestRels = struct {
	AppUser string
}{
	AppUser: "AppUser",
}

// subscriptionRequestR is where relationships are stored.
type subscriptionRequestR struct {
	AppUser *User `boil:"AppUser" json:"AppUser" toml:"AppUser" yaml:"AppUser"`
}

// NewStruct creates a new relationship struct
func (*subscriptionRequestR) NewStruct() *subscriptionRequestR {
	return &subscriptionRequestR{}
}

func (r *subscriptionRequestR) GetAppUser() *User {
	if r == nil {
		return nil
	}
	return r.AppUser
}

// subscriptionRequestL is where Load methods for each relationship are stored.
type subscriptionRequestL struct{}

var (
	subscriptionRequestAllColumns            = []string{"id", "user_id", "last_billing_update", "app_user_id"}
	subscriptionRequestColumnsWithoutDefault = []string{"user_id", "last_billing_update", "app_user_id"}
	subscriptionRequestColumnsWithDefault    = []string{"id"}
	subscriptionRequestPrimaryKeyColumns     = []string{"id"}
	subscriptionRequestGeneratedColumns      = []string{}
)

type (
	// SubscriptionRequestSlice is an alias for a slice of pointers to SubscriptionRequest.
	// This should almost always be used instead of []SubscriptionRequest.
	SubscriptionRequestSlice []*SubscriptionRequest

	subscriptionRequestQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	subscriptionRequestType                 = reflect.TypeOf(&SubscriptionRequest{})
	subscriptionRequestMapping              = queries.MakeStructMapping(subscriptionRequestType)
	subscriptionRequestPrimaryKeyMapping, _ = queries.BindMapping(subscriptionRequestType, subscriptionRequestMapping, subscriptionRequestPrimaryKeyColumns)
	subscriptionRequestInsertCacheMut       sync.RWMutex
	subscriptionRequestInsertCache          = make(map[string]insertCache)
	subscriptionRequestUpdateCacheMut       sync.RWMutex
	subscriptionRequestUpdateCache          = make(map[string]updateCache)
	subscriptionRequestUpsertCacheMut       sync.RWMutex
	subscriptionRequestUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single subscriptionRequest record from the query.
func (q subscriptionRequestQuery) One(exec boil.Executor) (*SubscriptionRequest, error) {
	o := &SubscriptionRequest{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: failed to execute a one query for subscription_requests")
	}

	return o, nil
}

// All returns all SubscriptionRequest records from the query.
func (q subscriptionRequestQuery) All(exec boil.Executor) (SubscriptionRequestSlice, error) {
	var o []*SubscriptionRequest

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "schema: failed to assign all query results to SubscriptionRequest slice")
	}

	return o, nil
}

// Count returns the count of all SubscriptionRequest records in the query.
func (q subscriptionRequestQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to count subscription_requests rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q subscriptionRequestQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "schema: failed to check if subscription_requests exists")
	}

	return count > 0, nil
}

// AppUser pointed to by the foreign key.
func (o *SubscriptionRequest) AppUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AppUserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadAppUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (subscriptionRequestL) LoadAppUser(e boil.Executor, singular bool, maybeSubscriptionRequest interface{}, mods queries.Applicator) error {
	var slice []*SubscriptionRequest
	var object *SubscriptionRequest

	if singular {
		var ok bool
		object, ok = maybeSubscriptionRequest.(*SubscriptionRequest)
		if !ok {
			object = new(SubscriptionRequest)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSubscriptionRequest)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSubscriptionRequest))
			}
		}
	} else {
		s, ok := maybeSubscriptionRequest.(*[]*SubscriptionRequest)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSubscriptionRequest)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSubscriptionRequest))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &subscriptionRequestR{}
		}
		args[object.AppUserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &subscriptionRequestR{}
			}

			args[obj.AppUserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AppUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.AppUserSubscriptionRequests = append(foreign.R.AppUserSubscriptionRequests, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AppUserID == foreign.ID {
				local.R.AppUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.AppUserSubscriptionRequests = append(foreign.R.AppUserSubscriptionRequests, local)
				break
			}
		}
	}

	return nil
}

// SetAppUser of the subscriptionRequest to the related item.
// Sets o.R.AppUser to related.
// Adds o to related.R.AppUserSubscriptionRequests.
func (o *SubscriptionRequest) SetAppUser(exec boil.Executor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"subscription_requests\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"app_user_id"}),
		strmangle.WhereClause("\"", "\"", 2, subscriptionRequestPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AppUserID = related.ID
	if o.R == nil {
		o.R = &subscriptionRequestR{
			AppUser: related,
		}
	} else {
		o.R.AppUser = related
	}

	if related.R == nil {
		related.R = &userR{
			AppUserSubscriptionRequests: SubscriptionRequestSlice{o},
		}
	} else {
		related.R.AppUserSubscriptionRequests = append(related.R.AppUserSubscriptionRequests, o)
	}

	return nil
}

// SubscriptionRequests retrieves all the records using an executor.
func SubscriptionRequests(mods ...qm.QueryMod) subscriptionRequestQuery {
	mods = append(mods, qm.From("\"subscription_requests\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"subscription_requests\".*"})
	}

	return subscriptionRequestQuery{q}
}

// FindSubscriptionRequest retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSubscriptionRequest(exec boil.Executor, iD int, selectCols ...string) (*SubscriptionRequest, error) {
	subscriptionRequestObj := &SubscriptionRequest{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"subscription_requests\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, subscriptionRequestObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: unable to select from subscription_requests")
	}

	return subscriptionRequestObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SubscriptionRequest) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("schema: no subscription_requests provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(subscriptionRequestColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	subscriptionRequestInsertCacheMut.RLock()
	cache, cached := subscriptionRequestInsertCache[key]
	subscriptionRequestInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			subscriptionRequestAllColumns,
			subscriptionRequestColumnsWithDefault,
			subscriptionRequestColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(subscriptionRequestType, subscriptionRequestMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(subscriptionRequestType, subscriptionRequestMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"subscription_requests\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"subscription_requests\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "schema: unable to insert into subscription_requests")
	}

	if !cached {
		subscriptionRequestInsertCacheMut.Lock()
		subscriptionRequestInsertCache[key] = cache
		subscriptionRequestInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the SubscriptionRequest.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SubscriptionRequest) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	subscriptionRequestUpdateCacheMut.RLock()
	cache, cached := subscriptionRequestUpdateCache[key]
	subscriptionRequestUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			subscriptionRequestAllColumns,
			subscriptionRequestPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("schema: unable to update subscription_requests, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"subscription_requests\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, subscriptionRequestPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(subscriptionRequestType, subscriptionRequestMapping, append(wl, subscriptionRequestPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update subscription_requests row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by update for subscription_requests")
	}

	if !cached {
		subscriptionRequestUpdateCacheMut.Lock()
		subscriptionRequestUpdateCache[key] = cache
		subscriptionRequestUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q subscriptionRequestQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all for subscription_requests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected for subscription_requests")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SubscriptionRequestSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("schema: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), subscriptionRequestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"subscription_requests\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, subscriptionRequestPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all in subscriptionRequest slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected all in update all subscriptionRequest")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SubscriptionRequest) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("schema: no subscription_requests provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(subscriptionRequestColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	subscriptionRequestUpsertCacheMut.RLock()
	cache, cached := subscriptionRequestUpsertCache[key]
	subscriptionRequestUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			subscriptionRequestAllColumns,
			subscriptionRequestColumnsWithDefault,
			subscriptionRequestColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			subscriptionRequestAllColumns,
			subscriptionRequestPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("schema: unable to upsert subscription_requests, could not build update column list")
		}

		ret := strmangle.SetComplement(subscriptionRequestAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(subscriptionRequestPrimaryKeyColumns) == 0 {
				return errors.New("schema: unable to upsert subscription_requests, could not build conflict column list")
			}

			conflict = make([]string, len(subscriptionRequestPrimaryKeyColumns))
			copy(conflict, subscriptionRequestPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"subscription_requests\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(subscriptionRequestType, subscriptionRequestMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(subscriptionRequestType, subscriptionRequestMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "schema: unable to upsert subscription_requests")
	}

	if !cached {
		subscriptionRequestUpsertCacheMut.Lock()
		subscriptionRequestUpsertCache[key] = cache
		subscriptionRequestUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single SubscriptionRequest record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SubscriptionRequest) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("schema: no SubscriptionRequest provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), subscriptionRequestPrimaryKeyMapping)
	sql := "DELETE FROM \"subscription_requests\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete from subscription_requests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by delete for subscription_requests")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q subscriptionRequestQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("schema: no subscriptionRequestQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from subscription_requests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for subscription_requests")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SubscriptionRequestSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), subscriptionRequestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"subscription_requests\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, subscriptionRequestPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from subscriptionRequest slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for subscription_requests")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SubscriptionRequest) Reload(exec boil.Executor) error {
	ret, err := FindSubscriptionRequest(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SubscriptionRequestSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SubscriptionRequestSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), subscriptionRequestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"subscription_requests\".* FROM \"subscription_requests\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, subscriptionRequestPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "schema: unable to reload all in SubscriptionRequestSlice")
	}

	*o = slice

	return nil
}

// SubscriptionRequestExists checks if the SubscriptionRequest row exists.
func SubscriptionRequestExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"subscription_requests\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "schema: unable to check if subscription_requests exists")
	}

	return exists, nil
}

// Exists checks if the SubscriptionRequest row exists.
func (o *SubscriptionRequest) Exists(exec boil.Executor) (bool, error) {
	return SubscriptionRequestExists(exec, o.ID)
}
