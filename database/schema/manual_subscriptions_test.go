// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testManualSubscriptions(t *testing.T) {
	t.Parallel()

	query := ManualSubscriptions()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testManualSubscriptionsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testManualSubscriptionsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ManualSubscriptions().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testManualSubscriptionsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ManualSubscriptionSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testManualSubscriptionsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ManualSubscriptionExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if ManualSubscription exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ManualSubscriptionExists to return true, but got false.")
	}
}

func testManualSubscriptionsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	manualSubscriptionFound, err := FindManualSubscription(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if manualSubscriptionFound == nil {
		t.Error("want a record, got nil")
	}
}

func testManualSubscriptionsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ManualSubscriptions().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testManualSubscriptionsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ManualSubscriptions().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testManualSubscriptionsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	manualSubscriptionOne := &ManualSubscription{}
	manualSubscriptionTwo := &ManualSubscription{}
	if err = randomize.Struct(seed, manualSubscriptionOne, manualSubscriptionDBTypes, false, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}
	if err = randomize.Struct(seed, manualSubscriptionTwo, manualSubscriptionDBTypes, false, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = manualSubscriptionOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = manualSubscriptionTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ManualSubscriptions().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testManualSubscriptionsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	manualSubscriptionOne := &ManualSubscription{}
	manualSubscriptionTwo := &ManualSubscription{}
	if err = randomize.Struct(seed, manualSubscriptionOne, manualSubscriptionDBTypes, false, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}
	if err = randomize.Struct(seed, manualSubscriptionTwo, manualSubscriptionDBTypes, false, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = manualSubscriptionOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = manualSubscriptionTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testManualSubscriptionsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testManualSubscriptionsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(manualSubscriptionColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testManualSubscriptionToOneUserUsingUser(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local ManualSubscription
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, manualSubscriptionDBTypes, false, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := ManualSubscriptionSlice{&local}
	if err = local.L.LoadUser(tx, false, (*[]*ManualSubscription)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testManualSubscriptionToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a ManualSubscription
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, manualSubscriptionDBTypes, false, strmangle.SetComplement(manualSubscriptionPrimaryKeyColumns, manualSubscriptionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ManualSubscriptions[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testManualSubscriptionsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testManualSubscriptionsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ManualSubscriptionSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testManualSubscriptionsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ManualSubscriptions().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	manualSubscriptionDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `Tier`: `integer`, `ExpiresAt`: `timestamp with time zone`, `UserID`: `uuid`}
	_                         = bytes.MinRead
)

func testManualSubscriptionsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(manualSubscriptionPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(manualSubscriptionAllColumns) == len(manualSubscriptionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testManualSubscriptionsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(manualSubscriptionAllColumns) == len(manualSubscriptionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ManualSubscription{}
	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, manualSubscriptionDBTypes, true, manualSubscriptionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(manualSubscriptionAllColumns, manualSubscriptionPrimaryKeyColumns) {
		fields = manualSubscriptionAllColumns
	} else {
		fields = strmangle.SetComplement(
			manualSubscriptionAllColumns,
			manualSubscriptionPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ManualSubscriptionSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testManualSubscriptionsUpsert(t *testing.T) {
	t.Parallel()

	if len(manualSubscriptionAllColumns) == len(manualSubscriptionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ManualSubscription{}
	if err = randomize.Struct(seed, &o, manualSubscriptionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ManualSubscription: %s", err)
	}

	count, err := ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, manualSubscriptionDBTypes, false, manualSubscriptionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ManualSubscription struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ManualSubscription: %s", err)
	}

	count, err = ManualSubscriptions().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
