// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testMatchRewards(t *testing.T) {
	t.Parallel()

	query := MatchRewards()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testMatchRewardsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchRewardsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := MatchRewards().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchRewardsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MatchRewardSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchRewardsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := MatchRewardExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if MatchReward exists: %s", err)
	}
	if !e {
		t.Errorf("Expected MatchRewardExists to return true, but got false.")
	}
}

func testMatchRewardsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	matchRewardFound, err := FindMatchReward(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if matchRewardFound == nil {
		t.Error("want a record, got nil")
	}
}

func testMatchRewardsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = MatchRewards().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testMatchRewardsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := MatchRewards().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testMatchRewardsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	matchRewardOne := &MatchReward{}
	matchRewardTwo := &MatchReward{}
	if err = randomize.Struct(seed, matchRewardOne, matchRewardDBTypes, false, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}
	if err = randomize.Struct(seed, matchRewardTwo, matchRewardDBTypes, false, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = matchRewardOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = matchRewardTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MatchRewards().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testMatchRewardsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	matchRewardOne := &MatchReward{}
	matchRewardTwo := &MatchReward{}
	if err = randomize.Struct(seed, matchRewardOne, matchRewardDBTypes, false, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}
	if err = randomize.Struct(seed, matchRewardTwo, matchRewardDBTypes, false, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = matchRewardOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = matchRewardTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testMatchRewardsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMatchRewardsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(matchRewardColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMatchRewardToOneMatchUsingMatch(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local MatchReward
	var foreign Match

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, matchRewardDBTypes, false, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, matchDBTypes, false, matchColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Match struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.MatchID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Match().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := MatchRewardSlice{&local}
	if err = local.L.LoadMatch(tx, false, (*[]*MatchReward)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Match == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Match = nil
	if err = local.L.LoadMatch(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Match == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testMatchRewardToOneSetOpMatchUsingMatch(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchReward
	var b, c Match

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchRewardDBTypes, false, strmangle.SetComplement(matchRewardPrimaryKeyColumns, matchRewardColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Match{&b, &c} {
		err = a.SetMatch(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Match != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.MatchRewards[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.MatchID != x.ID {
			t.Error("foreign key was wrong value", a.MatchID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.MatchID))
		reflect.Indirect(reflect.ValueOf(&a.MatchID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.MatchID != x.ID {
			t.Error("foreign key was wrong value", a.MatchID, x.ID)
		}
	}
}

func testMatchRewardsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testMatchRewardsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MatchRewardSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testMatchRewardsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MatchRewards().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	matchRewardDBTypes = map[string]string{`ID`: `integer`, `Position`: `integer`, `Amount`: `double precision`, `MatchID`: `uuid`, `MaxPosition`: `integer`, `MinPosition`: `integer`, `Balls`: `integer`, `Event`: `integer`, `Game`: `double precision`, `Lapt`: `double precision`, `Shirts`: `integer`, `SignedBalls`: `integer`, `SignedShirts`: `integer`, `KickoffPack1`: `integer`, `KickoffPack2`: `integer`, `KickoffPack3`: `integer`, `SeasonPack1`: `integer`, `SeasonPack2`: `integer`, `SeasonPack3`: `integer`}
	_                  = bytes.MinRead
)

func testMatchRewardsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(matchRewardPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(matchRewardAllColumns) == len(matchRewardPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testMatchRewardsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(matchRewardAllColumns) == len(matchRewardPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MatchReward{}
	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, matchRewardDBTypes, true, matchRewardPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(matchRewardAllColumns, matchRewardPrimaryKeyColumns) {
		fields = matchRewardAllColumns
	} else {
		fields = strmangle.SetComplement(
			matchRewardAllColumns,
			matchRewardPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := MatchRewardSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testMatchRewardsUpsert(t *testing.T) {
	t.Parallel()

	if len(matchRewardAllColumns) == len(matchRewardPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := MatchReward{}
	if err = randomize.Struct(seed, &o, matchRewardDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MatchReward: %s", err)
	}

	count, err := MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, matchRewardDBTypes, false, matchRewardPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchReward struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MatchReward: %s", err)
	}

	count, err = MatchRewards().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
