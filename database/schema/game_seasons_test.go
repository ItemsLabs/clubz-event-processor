// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testGameSeasons(t *testing.T) {
	t.Parallel()

	query := GameSeasons()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testGameSeasonsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGameSeasonsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := GameSeasons().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGameSeasonsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := GameSeasonSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testGameSeasonsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := GameSeasonExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if GameSeason exists: %s", err)
	}
	if !e {
		t.Errorf("Expected GameSeasonExists to return true, but got false.")
	}
}

func testGameSeasonsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	gameSeasonFound, err := FindGameSeason(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if gameSeasonFound == nil {
		t.Error("want a record, got nil")
	}
}

func testGameSeasonsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = GameSeasons().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testGameSeasonsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := GameSeasons().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testGameSeasonsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	gameSeasonOne := &GameSeason{}
	gameSeasonTwo := &GameSeason{}
	if err = randomize.Struct(seed, gameSeasonOne, gameSeasonDBTypes, false, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}
	if err = randomize.Struct(seed, gameSeasonTwo, gameSeasonDBTypes, false, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = gameSeasonOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = gameSeasonTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := GameSeasons().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testGameSeasonsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	gameSeasonOne := &GameSeason{}
	gameSeasonTwo := &GameSeason{}
	if err = randomize.Struct(seed, gameSeasonOne, gameSeasonDBTypes, false, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}
	if err = randomize.Struct(seed, gameSeasonTwo, gameSeasonDBTypes, false, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = gameSeasonOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = gameSeasonTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testGameSeasonsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testGameSeasonsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(gameSeasonColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testGameSeasonToManySeasonGameWeeks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a GameSeason
	var b, c GameWeek

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, gameWeekDBTypes, false, gameWeekColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, gameWeekDBTypes, false, gameWeekColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.SeasonID, a.ID)
	queries.Assign(&c.SeasonID, a.ID)
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.SeasonGameWeeks().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.SeasonID, b.SeasonID) {
			bFound = true
		}
		if queries.Equal(v.SeasonID, c.SeasonID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := GameSeasonSlice{&a}
	if err = a.L.LoadSeasonGameWeeks(tx, false, (*[]*GameSeason)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.SeasonGameWeeks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.SeasonGameWeeks = nil
	if err = a.L.LoadSeasonGameWeeks(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.SeasonGameWeeks); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testGameSeasonToManyAddOpSeasonGameWeeks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a GameSeason
	var b, c, d, e GameWeek

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, gameSeasonDBTypes, false, strmangle.SetComplement(gameSeasonPrimaryKeyColumns, gameSeasonColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*GameWeek{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, gameWeekDBTypes, false, strmangle.SetComplement(gameWeekPrimaryKeyColumns, gameWeekColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*GameWeek{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddSeasonGameWeeks(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.SeasonID) {
			t.Error("foreign key was wrong value", a.ID, first.SeasonID)
		}
		if !queries.Equal(a.ID, second.SeasonID) {
			t.Error("foreign key was wrong value", a.ID, second.SeasonID)
		}

		if first.R.Season != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Season != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.SeasonGameWeeks[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.SeasonGameWeeks[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.SeasonGameWeeks().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testGameSeasonToManySetOpSeasonGameWeeks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a GameSeason
	var b, c, d, e GameWeek

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, gameSeasonDBTypes, false, strmangle.SetComplement(gameSeasonPrimaryKeyColumns, gameSeasonColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*GameWeek{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, gameWeekDBTypes, false, strmangle.SetComplement(gameWeekPrimaryKeyColumns, gameWeekColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetSeasonGameWeeks(tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.SeasonGameWeeks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetSeasonGameWeeks(tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.SeasonGameWeeks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.SeasonID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.SeasonID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.SeasonID) {
		t.Error("foreign key was wrong value", a.ID, d.SeasonID)
	}
	if !queries.Equal(a.ID, e.SeasonID) {
		t.Error("foreign key was wrong value", a.ID, e.SeasonID)
	}

	if b.R.Season != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Season != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Season != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Season != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.SeasonGameWeeks[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.SeasonGameWeeks[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testGameSeasonToManyRemoveOpSeasonGameWeeks(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a GameSeason
	var b, c, d, e GameWeek

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, gameSeasonDBTypes, false, strmangle.SetComplement(gameSeasonPrimaryKeyColumns, gameSeasonColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*GameWeek{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, gameWeekDBTypes, false, strmangle.SetComplement(gameWeekPrimaryKeyColumns, gameWeekColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddSeasonGameWeeks(tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.SeasonGameWeeks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveSeasonGameWeeks(tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.SeasonGameWeeks().Count(tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.SeasonID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.SeasonID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Season != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Season != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Season != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Season != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.SeasonGameWeeks) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.SeasonGameWeeks[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.SeasonGameWeeks[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testGameSeasonsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testGameSeasonsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := GameSeasonSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testGameSeasonsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := GameSeasons().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	gameSeasonDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `ImportID`: `character varying`, `Name`: `text`, `StartAt`: `timestamp with time zone`, `EndAt`: `timestamp with time zone`}
	_                 = bytes.MinRead
)

func testGameSeasonsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(gameSeasonPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(gameSeasonAllColumns) == len(gameSeasonPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testGameSeasonsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(gameSeasonAllColumns) == len(gameSeasonPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &GameSeason{}
	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, gameSeasonDBTypes, true, gameSeasonPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(gameSeasonAllColumns, gameSeasonPrimaryKeyColumns) {
		fields = gameSeasonAllColumns
	} else {
		fields = strmangle.SetComplement(
			gameSeasonAllColumns,
			gameSeasonPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := GameSeasonSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testGameSeasonsUpsert(t *testing.T) {
	t.Parallel()

	if len(gameSeasonAllColumns) == len(gameSeasonPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := GameSeason{}
	if err = randomize.Struct(seed, &o, gameSeasonDBTypes, true); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert GameSeason: %s", err)
	}

	count, err := GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, gameSeasonDBTypes, false, gameSeasonPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize GameSeason struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert GameSeason: %s", err)
	}

	count, err = GameSeasons().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
