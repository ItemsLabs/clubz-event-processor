// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BanPenalty is an object representing the database table.
type BanPenalty struct {
	ID        int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	StartTime time.Time   `boil:"start_time" json:"start_time" toml:"start_time" yaml:"start_time"`
	EndTime   null.Time   `boil:"end_time" json:"end_time,omitempty" toml:"end_time" yaml:"end_time,omitempty"`
	Reason    null.String `boil:"reason" json:"reason,omitempty" toml:"reason" yaml:"reason,omitempty"`
	UserID    string      `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`

	R *banPenaltyR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L banPenaltyL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BanPenaltyColumns = struct {
	ID        string
	StartTime string
	EndTime   string
	Reason    string
	UserID    string
}{
	ID:        "id",
	StartTime: "start_time",
	EndTime:   "end_time",
	Reason:    "reason",
	UserID:    "user_id",
}

var BanPenaltyTableColumns = struct {
	ID        string
	StartTime string
	EndTime   string
	Reason    string
	UserID    string
}{
	ID:        "ban_penalties.id",
	StartTime: "ban_penalties.start_time",
	EndTime:   "ban_penalties.end_time",
	Reason:    "ban_penalties.reason",
	UserID:    "ban_penalties.user_id",
}

// Generated where

var BanPenaltyWhere = struct {
	ID        whereHelperint
	StartTime whereHelpertime_Time
	EndTime   whereHelpernull_Time
	Reason    whereHelpernull_String
	UserID    whereHelperstring
}{
	ID:        whereHelperint{field: "\"ban_penalties\".\"id\""},
	StartTime: whereHelpertime_Time{field: "\"ban_penalties\".\"start_time\""},
	EndTime:   whereHelpernull_Time{field: "\"ban_penalties\".\"end_time\""},
	Reason:    whereHelpernull_String{field: "\"ban_penalties\".\"reason\""},
	UserID:    whereHelperstring{field: "\"ban_penalties\".\"user_id\""},
}

// BanPenaltyRels is where relationship names are stored.
var BanPenaltyRels = struct {
	User string
}{
	User: "User",
}

// banPenaltyR is where relationships are stored.
type banPenaltyR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*banPenaltyR) NewStruct() *banPenaltyR {
	return &banPenaltyR{}
}

func (r *banPenaltyR) GetUser() *User {
	if r == nil {
		return nil
	}
	return r.User
}

// banPenaltyL is where Load methods for each relationship are stored.
type banPenaltyL struct{}

var (
	banPenaltyAllColumns            = []string{"id", "start_time", "end_time", "reason", "user_id"}
	banPenaltyColumnsWithoutDefault = []string{"start_time", "user_id"}
	banPenaltyColumnsWithDefault    = []string{"id", "end_time", "reason"}
	banPenaltyPrimaryKeyColumns     = []string{"id"}
	banPenaltyGeneratedColumns      = []string{}
)

type (
	// BanPenaltySlice is an alias for a slice of pointers to BanPenalty.
	// This should almost always be used instead of []BanPenalty.
	BanPenaltySlice []*BanPenalty

	banPenaltyQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	banPenaltyType                 = reflect.TypeOf(&BanPenalty{})
	banPenaltyMapping              = queries.MakeStructMapping(banPenaltyType)
	banPenaltyPrimaryKeyMapping, _ = queries.BindMapping(banPenaltyType, banPenaltyMapping, banPenaltyPrimaryKeyColumns)
	banPenaltyInsertCacheMut       sync.RWMutex
	banPenaltyInsertCache          = make(map[string]insertCache)
	banPenaltyUpdateCacheMut       sync.RWMutex
	banPenaltyUpdateCache          = make(map[string]updateCache)
	banPenaltyUpsertCacheMut       sync.RWMutex
	banPenaltyUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single banPenalty record from the query.
func (q banPenaltyQuery) One(exec boil.Executor) (*BanPenalty, error) {
	o := &BanPenalty{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: failed to execute a one query for ban_penalties")
	}

	return o, nil
}

// All returns all BanPenalty records from the query.
func (q banPenaltyQuery) All(exec boil.Executor) (BanPenaltySlice, error) {
	var o []*BanPenalty

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "schema: failed to assign all query results to BanPenalty slice")
	}

	return o, nil
}

// Count returns the count of all BanPenalty records in the query.
func (q banPenaltyQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to count ban_penalties rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q banPenaltyQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "schema: failed to check if ban_penalties exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *BanPenalty) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (banPenaltyL) LoadUser(e boil.Executor, singular bool, maybeBanPenalty interface{}, mods queries.Applicator) error {
	var slice []*BanPenalty
	var object *BanPenalty

	if singular {
		var ok bool
		object, ok = maybeBanPenalty.(*BanPenalty)
		if !ok {
			object = new(BanPenalty)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeBanPenalty)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeBanPenalty))
			}
		}
	} else {
		s, ok := maybeBanPenalty.(*[]*BanPenalty)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeBanPenalty)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeBanPenalty))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &banPenaltyR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &banPenaltyR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.BanPenalties = append(foreign.R.BanPenalties, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.BanPenalties = append(foreign.R.BanPenalties, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the banPenalty to the related item.
// Sets o.R.User to related.
// Adds o to related.R.BanPenalties.
func (o *BanPenalty) SetUser(exec boil.Executor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"ban_penalties\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, banPenaltyPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &banPenaltyR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			BanPenalties: BanPenaltySlice{o},
		}
	} else {
		related.R.BanPenalties = append(related.R.BanPenalties, o)
	}

	return nil
}

// BanPenalties retrieves all the records using an executor.
func BanPenalties(mods ...qm.QueryMod) banPenaltyQuery {
	mods = append(mods, qm.From("\"ban_penalties\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"ban_penalties\".*"})
	}

	return banPenaltyQuery{q}
}

// FindBanPenalty retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBanPenalty(exec boil.Executor, iD int, selectCols ...string) (*BanPenalty, error) {
	banPenaltyObj := &BanPenalty{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"ban_penalties\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, banPenaltyObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: unable to select from ban_penalties")
	}

	return banPenaltyObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BanPenalty) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("schema: no ban_penalties provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(banPenaltyColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	banPenaltyInsertCacheMut.RLock()
	cache, cached := banPenaltyInsertCache[key]
	banPenaltyInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			banPenaltyAllColumns,
			banPenaltyColumnsWithDefault,
			banPenaltyColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(banPenaltyType, banPenaltyMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(banPenaltyType, banPenaltyMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"ban_penalties\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"ban_penalties\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "schema: unable to insert into ban_penalties")
	}

	if !cached {
		banPenaltyInsertCacheMut.Lock()
		banPenaltyInsertCache[key] = cache
		banPenaltyInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the BanPenalty.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BanPenalty) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	banPenaltyUpdateCacheMut.RLock()
	cache, cached := banPenaltyUpdateCache[key]
	banPenaltyUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			banPenaltyAllColumns,
			banPenaltyPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("schema: unable to update ban_penalties, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"ban_penalties\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, banPenaltyPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(banPenaltyType, banPenaltyMapping, append(wl, banPenaltyPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update ban_penalties row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by update for ban_penalties")
	}

	if !cached {
		banPenaltyUpdateCacheMut.Lock()
		banPenaltyUpdateCache[key] = cache
		banPenaltyUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q banPenaltyQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all for ban_penalties")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected for ban_penalties")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BanPenaltySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("schema: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), banPenaltyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"ban_penalties\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, banPenaltyPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all in banPenalty slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected all in update all banPenalty")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BanPenalty) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("schema: no ban_penalties provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(banPenaltyColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	banPenaltyUpsertCacheMut.RLock()
	cache, cached := banPenaltyUpsertCache[key]
	banPenaltyUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			banPenaltyAllColumns,
			banPenaltyColumnsWithDefault,
			banPenaltyColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			banPenaltyAllColumns,
			banPenaltyPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("schema: unable to upsert ban_penalties, could not build update column list")
		}

		ret := strmangle.SetComplement(banPenaltyAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(banPenaltyPrimaryKeyColumns) == 0 {
				return errors.New("schema: unable to upsert ban_penalties, could not build conflict column list")
			}

			conflict = make([]string, len(banPenaltyPrimaryKeyColumns))
			copy(conflict, banPenaltyPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"ban_penalties\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(banPenaltyType, banPenaltyMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(banPenaltyType, banPenaltyMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "schema: unable to upsert ban_penalties")
	}

	if !cached {
		banPenaltyUpsertCacheMut.Lock()
		banPenaltyUpsertCache[key] = cache
		banPenaltyUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single BanPenalty record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BanPenalty) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("schema: no BanPenalty provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), banPenaltyPrimaryKeyMapping)
	sql := "DELETE FROM \"ban_penalties\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete from ban_penalties")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by delete for ban_penalties")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q banPenaltyQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("schema: no banPenaltyQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from ban_penalties")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for ban_penalties")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BanPenaltySlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), banPenaltyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"ban_penalties\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, banPenaltyPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from banPenalty slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for ban_penalties")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BanPenalty) Reload(exec boil.Executor) error {
	ret, err := FindBanPenalty(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BanPenaltySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BanPenaltySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), banPenaltyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"ban_penalties\".* FROM \"ban_penalties\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, banPenaltyPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "schema: unable to reload all in BanPenaltySlice")
	}

	*o = slice

	return nil
}

// BanPenaltyExists checks if the BanPenalty row exists.
func BanPenaltyExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"ban_penalties\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "schema: unable to check if ban_penalties exists")
	}

	return exists, nil
}

// Exists checks if the BanPenalty row exists.
func (o *BanPenalty) Exists(exec boil.Executor) (bool, error) {
	return BanPenaltyExists(exec, o.ID)
}
