// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// FanclashCache is an object representing the database table.
type FanclashCache struct {
	CacheKey string    `boil:"cache_key" json:"cache_key" toml:"cache_key" yaml:"cache_key"`
	Value    string    `boil:"value" json:"value" toml:"value" yaml:"value"`
	Expires  time.Time `boil:"expires" json:"expires" toml:"expires" yaml:"expires"`

	R *fanclashCacheR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L fanclashCacheL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FanclashCacheColumns = struct {
	CacheKey string
	Value    string
	Expires  string
}{
	CacheKey: "cache_key",
	Value:    "value",
	Expires:  "expires",
}

var FanclashCacheTableColumns = struct {
	CacheKey string
	Value    string
	Expires  string
}{
	CacheKey: "fanclash_cache.cache_key",
	Value:    "fanclash_cache.value",
	Expires:  "fanclash_cache.expires",
}

// Generated where

var FanclashCacheWhere = struct {
	CacheKey whereHelperstring
	Value    whereHelperstring
	Expires  whereHelpertime_Time
}{
	CacheKey: whereHelperstring{field: "\"fanclash_cache\".\"cache_key\""},
	Value:    whereHelperstring{field: "\"fanclash_cache\".\"value\""},
	Expires:  whereHelpertime_Time{field: "\"fanclash_cache\".\"expires\""},
}

// FanclashCacheRels is where relationship names are stored.
var FanclashCacheRels = struct {
}{}

// fanclashCacheR is where relationships are stored.
type fanclashCacheR struct {
}

// NewStruct creates a new relationship struct
func (*fanclashCacheR) NewStruct() *fanclashCacheR {
	return &fanclashCacheR{}
}

// fanclashCacheL is where Load methods for each relationship are stored.
type fanclashCacheL struct{}

var (
	fanclashCacheAllColumns            = []string{"cache_key", "value", "expires"}
	fanclashCacheColumnsWithoutDefault = []string{"cache_key", "value", "expires"}
	fanclashCacheColumnsWithDefault    = []string{}
	fanclashCachePrimaryKeyColumns     = []string{"cache_key"}
	fanclashCacheGeneratedColumns      = []string{}
)

type (
	// FanclashCacheSlice is an alias for a slice of pointers to FanclashCache.
	// This should almost always be used instead of []FanclashCache.
	FanclashCacheSlice []*FanclashCache

	fanclashCacheQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	fanclashCacheType                 = reflect.TypeOf(&FanclashCache{})
	fanclashCacheMapping              = queries.MakeStructMapping(fanclashCacheType)
	fanclashCachePrimaryKeyMapping, _ = queries.BindMapping(fanclashCacheType, fanclashCacheMapping, fanclashCachePrimaryKeyColumns)
	fanclashCacheInsertCacheMut       sync.RWMutex
	fanclashCacheInsertCache          = make(map[string]insertCache)
	fanclashCacheUpdateCacheMut       sync.RWMutex
	fanclashCacheUpdateCache          = make(map[string]updateCache)
	fanclashCacheUpsertCacheMut       sync.RWMutex
	fanclashCacheUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single fanclashCache record from the query.
func (q fanclashCacheQuery) One(exec boil.Executor) (*FanclashCache, error) {
	o := &FanclashCache{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: failed to execute a one query for fanclash_cache")
	}

	return o, nil
}

// All returns all FanclashCache records from the query.
func (q fanclashCacheQuery) All(exec boil.Executor) (FanclashCacheSlice, error) {
	var o []*FanclashCache

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "schema: failed to assign all query results to FanclashCache slice")
	}

	return o, nil
}

// Count returns the count of all FanclashCache records in the query.
func (q fanclashCacheQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to count fanclash_cache rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q fanclashCacheQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "schema: failed to check if fanclash_cache exists")
	}

	return count > 0, nil
}

// FanclashCaches retrieves all the records using an executor.
func FanclashCaches(mods ...qm.QueryMod) fanclashCacheQuery {
	mods = append(mods, qm.From("\"fanclash_cache\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"fanclash_cache\".*"})
	}

	return fanclashCacheQuery{q}
}

// FindFanclashCache retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFanclashCache(exec boil.Executor, cacheKey string, selectCols ...string) (*FanclashCache, error) {
	fanclashCacheObj := &FanclashCache{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"fanclash_cache\" where \"cache_key\"=$1", sel,
	)

	q := queries.Raw(query, cacheKey)

	err := q.Bind(nil, exec, fanclashCacheObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: unable to select from fanclash_cache")
	}

	return fanclashCacheObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FanclashCache) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("schema: no fanclash_cache provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(fanclashCacheColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	fanclashCacheInsertCacheMut.RLock()
	cache, cached := fanclashCacheInsertCache[key]
	fanclashCacheInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			fanclashCacheAllColumns,
			fanclashCacheColumnsWithDefault,
			fanclashCacheColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(fanclashCacheType, fanclashCacheMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(fanclashCacheType, fanclashCacheMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"fanclash_cache\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"fanclash_cache\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "schema: unable to insert into fanclash_cache")
	}

	if !cached {
		fanclashCacheInsertCacheMut.Lock()
		fanclashCacheInsertCache[key] = cache
		fanclashCacheInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the FanclashCache.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FanclashCache) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	fanclashCacheUpdateCacheMut.RLock()
	cache, cached := fanclashCacheUpdateCache[key]
	fanclashCacheUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			fanclashCacheAllColumns,
			fanclashCachePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("schema: unable to update fanclash_cache, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"fanclash_cache\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, fanclashCachePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(fanclashCacheType, fanclashCacheMapping, append(wl, fanclashCachePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update fanclash_cache row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by update for fanclash_cache")
	}

	if !cached {
		fanclashCacheUpdateCacheMut.Lock()
		fanclashCacheUpdateCache[key] = cache
		fanclashCacheUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q fanclashCacheQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all for fanclash_cache")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected for fanclash_cache")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FanclashCacheSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("schema: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), fanclashCachePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"fanclash_cache\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, fanclashCachePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all in fanclashCache slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected all in update all fanclashCache")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FanclashCache) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("schema: no fanclash_cache provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(fanclashCacheColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	fanclashCacheUpsertCacheMut.RLock()
	cache, cached := fanclashCacheUpsertCache[key]
	fanclashCacheUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			fanclashCacheAllColumns,
			fanclashCacheColumnsWithDefault,
			fanclashCacheColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			fanclashCacheAllColumns,
			fanclashCachePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("schema: unable to upsert fanclash_cache, could not build update column list")
		}

		ret := strmangle.SetComplement(fanclashCacheAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(fanclashCachePrimaryKeyColumns) == 0 {
				return errors.New("schema: unable to upsert fanclash_cache, could not build conflict column list")
			}

			conflict = make([]string, len(fanclashCachePrimaryKeyColumns))
			copy(conflict, fanclashCachePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"fanclash_cache\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(fanclashCacheType, fanclashCacheMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(fanclashCacheType, fanclashCacheMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "schema: unable to upsert fanclash_cache")
	}

	if !cached {
		fanclashCacheUpsertCacheMut.Lock()
		fanclashCacheUpsertCache[key] = cache
		fanclashCacheUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single FanclashCache record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FanclashCache) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("schema: no FanclashCache provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), fanclashCachePrimaryKeyMapping)
	sql := "DELETE FROM \"fanclash_cache\" WHERE \"cache_key\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete from fanclash_cache")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by delete for fanclash_cache")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q fanclashCacheQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("schema: no fanclashCacheQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from fanclash_cache")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for fanclash_cache")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FanclashCacheSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), fanclashCachePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"fanclash_cache\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, fanclashCachePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from fanclashCache slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for fanclash_cache")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FanclashCache) Reload(exec boil.Executor) error {
	ret, err := FindFanclashCache(exec, o.CacheKey)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FanclashCacheSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FanclashCacheSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), fanclashCachePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"fanclash_cache\".* FROM \"fanclash_cache\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, fanclashCachePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "schema: unable to reload all in FanclashCacheSlice")
	}

	*o = slice

	return nil
}

// FanclashCacheExists checks if the FanclashCache row exists.
func FanclashCacheExists(exec boil.Executor, cacheKey string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"fanclash_cache\" where \"cache_key\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, cacheKey)
	}
	row := exec.QueryRow(sql, cacheKey)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "schema: unable to check if fanclash_cache exists")
	}

	return exists, nil
}

// Exists checks if the FanclashCache row exists.
func (o *FanclashCache) Exists(exec boil.Executor) (bool, error) {
	return FanclashCacheExists(exec, o.CacheKey)
}
