// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserGameWeekHistories(t *testing.T) {
	t.Parallel()

	query := UserGameWeekHistories()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserGameWeekHistoriesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserGameWeekHistoriesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserGameWeekHistories().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserGameWeekHistoriesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserGameWeekHistorySlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserGameWeekHistoriesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserGameWeekHistoryExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserGameWeekHistory exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserGameWeekHistoryExists to return true, but got false.")
	}
}

func testUserGameWeekHistoriesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userGameWeekHistoryFound, err := FindUserGameWeekHistory(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userGameWeekHistoryFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserGameWeekHistoriesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserGameWeekHistories().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserGameWeekHistoriesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserGameWeekHistories().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserGameWeekHistoriesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userGameWeekHistoryOne := &UserGameWeekHistory{}
	userGameWeekHistoryTwo := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, userGameWeekHistoryOne, userGameWeekHistoryDBTypes, false, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}
	if err = randomize.Struct(seed, userGameWeekHistoryTwo, userGameWeekHistoryDBTypes, false, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = userGameWeekHistoryOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userGameWeekHistoryTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserGameWeekHistories().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserGameWeekHistoriesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userGameWeekHistoryOne := &UserGameWeekHistory{}
	userGameWeekHistoryTwo := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, userGameWeekHistoryOne, userGameWeekHistoryDBTypes, false, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}
	if err = randomize.Struct(seed, userGameWeekHistoryTwo, userGameWeekHistoryDBTypes, false, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = userGameWeekHistoryOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userGameWeekHistoryTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testUserGameWeekHistoriesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserGameWeekHistoriesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(userGameWeekHistoryColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserGameWeekHistoryToOneDivisionUsingNewDivision(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local UserGameWeekHistory
	var foreign Division

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, divisionDBTypes, false, divisionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Division struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.NewDivisionID, foreign.ID)
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.NewDivision().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserGameWeekHistorySlice{&local}
	if err = local.L.LoadNewDivision(tx, false, (*[]*UserGameWeekHistory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.NewDivision == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.NewDivision = nil
	if err = local.L.LoadNewDivision(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.NewDivision == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testUserGameWeekHistoryToOneDivisionUsingWeekDivision(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local UserGameWeekHistory
	var foreign Division

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, divisionDBTypes, false, divisionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Division struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.WeekDivisionID, foreign.ID)
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.WeekDivision().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserGameWeekHistorySlice{&local}
	if err = local.L.LoadWeekDivision(tx, false, (*[]*UserGameWeekHistory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.WeekDivision == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.WeekDivision = nil
	if err = local.L.LoadWeekDivision(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.WeekDivision == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testUserGameWeekHistoryToOneGameWeekUsingGameWeek(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local UserGameWeekHistory
	var foreign GameWeek

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userGameWeekHistoryDBTypes, false, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, gameWeekDBTypes, false, gameWeekColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize GameWeek struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.GameWeekID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.GameWeek().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserGameWeekHistorySlice{&local}
	if err = local.L.LoadGameWeek(tx, false, (*[]*UserGameWeekHistory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.GameWeek == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.GameWeek = nil
	if err = local.L.LoadGameWeek(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.GameWeek == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testUserGameWeekHistoryToOneUserUsingUser(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local UserGameWeekHistory
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userGameWeekHistoryDBTypes, false, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := UserGameWeekHistorySlice{&local}
	if err = local.L.LoadUser(tx, false, (*[]*UserGameWeekHistory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testUserGameWeekHistoryToOneSetOpDivisionUsingNewDivision(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserGameWeekHistory
	var b, c Division

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userGameWeekHistoryDBTypes, false, strmangle.SetComplement(userGameWeekHistoryPrimaryKeyColumns, userGameWeekHistoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, divisionDBTypes, false, strmangle.SetComplement(divisionPrimaryKeyColumns, divisionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, divisionDBTypes, false, strmangle.SetComplement(divisionPrimaryKeyColumns, divisionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Division{&b, &c} {
		err = a.SetNewDivision(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.NewDivision != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.NewDivisionUserGameWeekHistories[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.NewDivisionID, x.ID) {
			t.Error("foreign key was wrong value", a.NewDivisionID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.NewDivisionID))
		reflect.Indirect(reflect.ValueOf(&a.NewDivisionID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.NewDivisionID, x.ID) {
			t.Error("foreign key was wrong value", a.NewDivisionID, x.ID)
		}
	}
}

func testUserGameWeekHistoryToOneRemoveOpDivisionUsingNewDivision(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserGameWeekHistory
	var b Division

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userGameWeekHistoryDBTypes, false, strmangle.SetComplement(userGameWeekHistoryPrimaryKeyColumns, userGameWeekHistoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, divisionDBTypes, false, strmangle.SetComplement(divisionPrimaryKeyColumns, divisionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetNewDivision(tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveNewDivision(tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.NewDivision().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.NewDivision != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.NewDivisionID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.NewDivisionUserGameWeekHistories) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testUserGameWeekHistoryToOneSetOpDivisionUsingWeekDivision(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserGameWeekHistory
	var b, c Division

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userGameWeekHistoryDBTypes, false, strmangle.SetComplement(userGameWeekHistoryPrimaryKeyColumns, userGameWeekHistoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, divisionDBTypes, false, strmangle.SetComplement(divisionPrimaryKeyColumns, divisionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, divisionDBTypes, false, strmangle.SetComplement(divisionPrimaryKeyColumns, divisionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Division{&b, &c} {
		err = a.SetWeekDivision(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.WeekDivision != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.WeekDivisionUserGameWeekHistories[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.WeekDivisionID, x.ID) {
			t.Error("foreign key was wrong value", a.WeekDivisionID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.WeekDivisionID))
		reflect.Indirect(reflect.ValueOf(&a.WeekDivisionID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.WeekDivisionID, x.ID) {
			t.Error("foreign key was wrong value", a.WeekDivisionID, x.ID)
		}
	}
}

func testUserGameWeekHistoryToOneRemoveOpDivisionUsingWeekDivision(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserGameWeekHistory
	var b Division

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userGameWeekHistoryDBTypes, false, strmangle.SetComplement(userGameWeekHistoryPrimaryKeyColumns, userGameWeekHistoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, divisionDBTypes, false, strmangle.SetComplement(divisionPrimaryKeyColumns, divisionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetWeekDivision(tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveWeekDivision(tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.WeekDivision().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.WeekDivision != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.WeekDivisionID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.WeekDivisionUserGameWeekHistories) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testUserGameWeekHistoryToOneSetOpGameWeekUsingGameWeek(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserGameWeekHistory
	var b, c GameWeek

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userGameWeekHistoryDBTypes, false, strmangle.SetComplement(userGameWeekHistoryPrimaryKeyColumns, userGameWeekHistoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, gameWeekDBTypes, false, strmangle.SetComplement(gameWeekPrimaryKeyColumns, gameWeekColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, gameWeekDBTypes, false, strmangle.SetComplement(gameWeekPrimaryKeyColumns, gameWeekColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*GameWeek{&b, &c} {
		err = a.SetGameWeek(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.GameWeek != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserGameWeekHistories[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.GameWeekID != x.ID {
			t.Error("foreign key was wrong value", a.GameWeekID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.GameWeekID))
		reflect.Indirect(reflect.ValueOf(&a.GameWeekID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.GameWeekID != x.ID {
			t.Error("foreign key was wrong value", a.GameWeekID, x.ID)
		}
	}
}
func testUserGameWeekHistoryToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a UserGameWeekHistory
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userGameWeekHistoryDBTypes, false, strmangle.SetComplement(userGameWeekHistoryPrimaryKeyColumns, userGameWeekHistoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserGameWeekHistories[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testUserGameWeekHistoriesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testUserGameWeekHistoriesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserGameWeekHistorySlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testUserGameWeekHistoriesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserGameWeekHistories().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userGameWeekHistoryDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `WeekDivisionPosition`: `integer`, `WeekDivisionTier`: `integer`, `WeekPoints`: `integer`, `WeekCoins`: `integer`, `NewDivisionTier`: `integer`, `Status`: `character varying`, `GameWeekID`: `uuid`, `NewDivisionID`: `uuid`, `UserID`: `uuid`, `WeekDivisionID`: `uuid`, `WeekAverageRank`: `double precision`, `WeekMatchesPlayed`: `integer`}
	_                          = bytes.MinRead
)

func testUserGameWeekHistoriesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userGameWeekHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userGameWeekHistoryAllColumns) == len(userGameWeekHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserGameWeekHistoriesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userGameWeekHistoryAllColumns) == len(userGameWeekHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserGameWeekHistory{}
	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userGameWeekHistoryDBTypes, true, userGameWeekHistoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userGameWeekHistoryAllColumns, userGameWeekHistoryPrimaryKeyColumns) {
		fields = userGameWeekHistoryAllColumns
	} else {
		fields = strmangle.SetComplement(
			userGameWeekHistoryAllColumns,
			userGameWeekHistoryPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserGameWeekHistorySlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserGameWeekHistoriesUpsert(t *testing.T) {
	t.Parallel()

	if len(userGameWeekHistoryAllColumns) == len(userGameWeekHistoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserGameWeekHistory{}
	if err = randomize.Struct(seed, &o, userGameWeekHistoryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserGameWeekHistory: %s", err)
	}

	count, err := UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userGameWeekHistoryDBTypes, false, userGameWeekHistoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserGameWeekHistory struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserGameWeekHistory: %s", err)
	}

	count, err = UserGameWeekHistories().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
