// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testCompetitionPhases(t *testing.T) {
	t.Parallel()

	query := CompetitionPhases()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testCompetitionPhasesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCompetitionPhasesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := CompetitionPhases().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCompetitionPhasesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CompetitionPhaseSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCompetitionPhasesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := CompetitionPhaseExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if CompetitionPhase exists: %s", err)
	}
	if !e {
		t.Errorf("Expected CompetitionPhaseExists to return true, but got false.")
	}
}

func testCompetitionPhasesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	competitionPhaseFound, err := FindCompetitionPhase(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if competitionPhaseFound == nil {
		t.Error("want a record, got nil")
	}
}

func testCompetitionPhasesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = CompetitionPhases().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testCompetitionPhasesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := CompetitionPhases().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testCompetitionPhasesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	competitionPhaseOne := &CompetitionPhase{}
	competitionPhaseTwo := &CompetitionPhase{}
	if err = randomize.Struct(seed, competitionPhaseOne, competitionPhaseDBTypes, false, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}
	if err = randomize.Struct(seed, competitionPhaseTwo, competitionPhaseDBTypes, false, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = competitionPhaseOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = competitionPhaseTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CompetitionPhases().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testCompetitionPhasesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	competitionPhaseOne := &CompetitionPhase{}
	competitionPhaseTwo := &CompetitionPhase{}
	if err = randomize.Struct(seed, competitionPhaseOne, competitionPhaseDBTypes, false, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}
	if err = randomize.Struct(seed, competitionPhaseTwo, competitionPhaseDBTypes, false, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = competitionPhaseOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = competitionPhaseTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testCompetitionPhasesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCompetitionPhasesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(competitionPhaseColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCompetitionPhaseToOneCompetitionEditionUsingCompetitionEdition(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local CompetitionPhase
	var foreign CompetitionEdition

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, competitionPhaseDBTypes, false, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, competitionEditionDBTypes, false, competitionEditionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionEdition struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.CompetitionEditionID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.CompetitionEdition().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := CompetitionPhaseSlice{&local}
	if err = local.L.LoadCompetitionEdition(tx, false, (*[]*CompetitionPhase)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.CompetitionEdition == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.CompetitionEdition = nil
	if err = local.L.LoadCompetitionEdition(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.CompetitionEdition == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testCompetitionPhaseToOneSetOpCompetitionEditionUsingCompetitionEdition(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a CompetitionPhase
	var b, c CompetitionEdition

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, competitionPhaseDBTypes, false, strmangle.SetComplement(competitionPhasePrimaryKeyColumns, competitionPhaseColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, competitionEditionDBTypes, false, strmangle.SetComplement(competitionEditionPrimaryKeyColumns, competitionEditionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, competitionEditionDBTypes, false, strmangle.SetComplement(competitionEditionPrimaryKeyColumns, competitionEditionColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*CompetitionEdition{&b, &c} {
		err = a.SetCompetitionEdition(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.CompetitionEdition != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.CompetitionPhases[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.CompetitionEditionID != x.ID {
			t.Error("foreign key was wrong value", a.CompetitionEditionID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.CompetitionEditionID))
		reflect.Indirect(reflect.ValueOf(&a.CompetitionEditionID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.CompetitionEditionID != x.ID {
			t.Error("foreign key was wrong value", a.CompetitionEditionID, x.ID)
		}
	}
}

func testCompetitionPhasesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testCompetitionPhasesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CompetitionPhaseSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testCompetitionPhasesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CompetitionPhases().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	competitionPhaseDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `ImportID`: `character varying`, `Name`: `text`, `Enabled`: `boolean`, `CompetitionEditionID`: `uuid`}
	_                       = bytes.MinRead
)

func testCompetitionPhasesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(competitionPhasePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(competitionPhaseAllColumns) == len(competitionPhasePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhasePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testCompetitionPhasesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(competitionPhaseAllColumns) == len(competitionPhasePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CompetitionPhase{}
	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhaseColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, competitionPhaseDBTypes, true, competitionPhasePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(competitionPhaseAllColumns, competitionPhasePrimaryKeyColumns) {
		fields = competitionPhaseAllColumns
	} else {
		fields = strmangle.SetComplement(
			competitionPhaseAllColumns,
			competitionPhasePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := CompetitionPhaseSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testCompetitionPhasesUpsert(t *testing.T) {
	t.Parallel()

	if len(competitionPhaseAllColumns) == len(competitionPhasePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := CompetitionPhase{}
	if err = randomize.Struct(seed, &o, competitionPhaseDBTypes, true); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CompetitionPhase: %s", err)
	}

	count, err := CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, competitionPhaseDBTypes, false, competitionPhasePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CompetitionPhase struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CompetitionPhase: %s", err)
	}

	count, err = CompetitionPhases().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
