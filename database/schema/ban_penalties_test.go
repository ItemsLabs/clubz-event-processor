// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBanPenalties(t *testing.T) {
	t.Parallel()

	query := BanPenalties()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBanPenaltiesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBanPenaltiesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := BanPenalties().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBanPenaltiesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BanPenaltySlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBanPenaltiesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BanPenaltyExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if BanPenalty exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BanPenaltyExists to return true, but got false.")
	}
}

func testBanPenaltiesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	banPenaltyFound, err := FindBanPenalty(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if banPenaltyFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBanPenaltiesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = BanPenalties().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testBanPenaltiesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := BanPenalties().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBanPenaltiesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	banPenaltyOne := &BanPenalty{}
	banPenaltyTwo := &BanPenalty{}
	if err = randomize.Struct(seed, banPenaltyOne, banPenaltyDBTypes, false, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}
	if err = randomize.Struct(seed, banPenaltyTwo, banPenaltyDBTypes, false, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = banPenaltyOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = banPenaltyTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := BanPenalties().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBanPenaltiesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	banPenaltyOne := &BanPenalty{}
	banPenaltyTwo := &BanPenalty{}
	if err = randomize.Struct(seed, banPenaltyOne, banPenaltyDBTypes, false, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}
	if err = randomize.Struct(seed, banPenaltyTwo, banPenaltyDBTypes, false, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = banPenaltyOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = banPenaltyTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testBanPenaltiesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBanPenaltiesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(banPenaltyColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBanPenaltyToOneUserUsingUser(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local BanPenalty
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, banPenaltyDBTypes, false, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := BanPenaltySlice{&local}
	if err = local.L.LoadUser(tx, false, (*[]*BanPenalty)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testBanPenaltyToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a BanPenalty
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, banPenaltyDBTypes, false, strmangle.SetComplement(banPenaltyPrimaryKeyColumns, banPenaltyColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.BanPenalties[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.ID {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testBanPenaltiesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testBanPenaltiesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BanPenaltySlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testBanPenaltiesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := BanPenalties().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	banPenaltyDBTypes = map[string]string{`ID`: `integer`, `StartTime`: `timestamp with time zone`, `EndTime`: `timestamp with time zone`, `Reason`: `text`, `UserID`: `uuid`}
	_                 = bytes.MinRead
)

func testBanPenaltiesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(banPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(banPenaltyAllColumns) == len(banPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBanPenaltiesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(banPenaltyAllColumns) == len(banPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &BanPenalty{}
	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, banPenaltyDBTypes, true, banPenaltyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(banPenaltyAllColumns, banPenaltyPrimaryKeyColumns) {
		fields = banPenaltyAllColumns
	} else {
		fields = strmangle.SetComplement(
			banPenaltyAllColumns,
			banPenaltyPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BanPenaltySlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBanPenaltiesUpsert(t *testing.T) {
	t.Parallel()

	if len(banPenaltyAllColumns) == len(banPenaltyPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := BanPenalty{}
	if err = randomize.Struct(seed, &o, banPenaltyDBTypes, true); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert BanPenalty: %s", err)
	}

	count, err := BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, banPenaltyDBTypes, false, banPenaltyPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BanPenalty struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert BanPenalty: %s", err)
	}

	count, err = BanPenalties().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
