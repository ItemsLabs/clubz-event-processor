// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testMatchNotifications(t *testing.T) {
	t.Parallel()

	query := MatchNotifications()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testMatchNotificationsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchNotificationsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := MatchNotifications().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchNotificationsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MatchNotificationSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testMatchNotificationsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := MatchNotificationExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if MatchNotification exists: %s", err)
	}
	if !e {
		t.Errorf("Expected MatchNotificationExists to return true, but got false.")
	}
}

func testMatchNotificationsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	matchNotificationFound, err := FindMatchNotification(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if matchNotificationFound == nil {
		t.Error("want a record, got nil")
	}
}

func testMatchNotificationsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = MatchNotifications().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testMatchNotificationsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := MatchNotifications().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testMatchNotificationsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	matchNotificationOne := &MatchNotification{}
	matchNotificationTwo := &MatchNotification{}
	if err = randomize.Struct(seed, matchNotificationOne, matchNotificationDBTypes, false, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}
	if err = randomize.Struct(seed, matchNotificationTwo, matchNotificationDBTypes, false, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = matchNotificationOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = matchNotificationTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MatchNotifications().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testMatchNotificationsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	matchNotificationOne := &MatchNotification{}
	matchNotificationTwo := &MatchNotification{}
	if err = randomize.Struct(seed, matchNotificationOne, matchNotificationDBTypes, false, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}
	if err = randomize.Struct(seed, matchNotificationTwo, matchNotificationDBTypes, false, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = matchNotificationOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = matchNotificationTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testMatchNotificationsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMatchNotificationsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(matchNotificationColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testMatchNotificationToOneMatchUsingMatch(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local MatchNotification
	var foreign Match

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, matchNotificationDBTypes, false, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, matchDBTypes, false, matchColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Match struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.MatchID = foreign.ID
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Match().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := MatchNotificationSlice{&local}
	if err = local.L.LoadMatch(tx, false, (*[]*MatchNotification)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Match == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Match = nil
	if err = local.L.LoadMatch(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Match == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testMatchNotificationToOneUserUsingUser(t *testing.T) {

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var local MatchNotification
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.UserID, foreign.ID)
	if err := local.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := MatchNotificationSlice{&local}
	if err = local.L.LoadUser(tx, false, (*[]*MatchNotification)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

}

func testMatchNotificationToOneSetOpMatchUsingMatch(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchNotification
	var b, c Match

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchNotificationDBTypes, false, strmangle.SetComplement(matchNotificationPrimaryKeyColumns, matchNotificationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, matchDBTypes, false, strmangle.SetComplement(matchPrimaryKeyColumns, matchColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Match{&b, &c} {
		err = a.SetMatch(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Match != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.MatchNotifications[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.MatchID != x.ID {
			t.Error("foreign key was wrong value", a.MatchID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.MatchID))
		reflect.Indirect(reflect.ValueOf(&a.MatchID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.MatchID != x.ID {
			t.Error("foreign key was wrong value", a.MatchID, x.ID)
		}
	}
}
func testMatchNotificationToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchNotification
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchNotificationDBTypes, false, strmangle.SetComplement(matchNotificationPrimaryKeyColumns, matchNotificationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.MatchNotifications[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.UserID, x.ID) {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.UserID, x.ID) {
			t.Error("foreign key was wrong value", a.UserID, x.ID)
		}
	}
}

func testMatchNotificationToOneRemoveOpUserUsingUser(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a MatchNotification
	var b User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, matchNotificationDBTypes, false, strmangle.SetComplement(matchNotificationPrimaryKeyColumns, matchNotificationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetUser(tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveUser(tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.User().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.User != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.UserID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.MatchNotifications) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testMatchNotificationsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testMatchNotificationsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := MatchNotificationSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testMatchNotificationsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := MatchNotifications().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	matchNotificationDBTypes = map[string]string{`ID`: `uuid`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `Type`: `integer`, `MatchID`: `uuid`, `UserID`: `uuid`}
	_                        = bytes.MinRead
)

func testMatchNotificationsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(matchNotificationPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(matchNotificationAllColumns) == len(matchNotificationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testMatchNotificationsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(matchNotificationAllColumns) == len(matchNotificationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &MatchNotification{}
	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, matchNotificationDBTypes, true, matchNotificationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(matchNotificationAllColumns, matchNotificationPrimaryKeyColumns) {
		fields = matchNotificationAllColumns
	} else {
		fields = strmangle.SetComplement(
			matchNotificationAllColumns,
			matchNotificationPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := MatchNotificationSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testMatchNotificationsUpsert(t *testing.T) {
	t.Parallel()

	if len(matchNotificationAllColumns) == len(matchNotificationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := MatchNotification{}
	if err = randomize.Struct(seed, &o, matchNotificationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MatchNotification: %s", err)
	}

	count, err := MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, matchNotificationDBTypes, false, matchNotificationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize MatchNotification struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert MatchNotification: %s", err)
	}

	count, err = MatchNotifications().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
